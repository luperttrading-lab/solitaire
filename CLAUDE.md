# Solitaire – Steckbrett-Solitär als Spiel- und Lernapp (iPhone, GitHub Pages)

Stand: v1.61 (20.09.2026). Eine einzige Datei `index.html`, kein Build, keine Abhängigkeiten.
Autor/Product Owner: Lutz („Neo"). Sprache im Chat und in der App: Deutsch, per Du.

## Was die App kann
- 6 Bretter (Englisch 33, Europäisch 37 mit Start (0,2), Wiegleb 45, Dreieck 15/21, Quadrat 25), alle mit verifizierter 1-Stein-Lösung (Eröffnungsbuch `OPENING_BOOK`).
- Regelkonforme Züge, Auto-Sprung bei eindeutigem Zug, Wischen/Zielfeld tippen, Zurück/Vor mit animiertem Spulen (650 ms, erst Markierung, 400 ms später der Sprung), Zeit/Zähler, Bestleistungen, Startbild 2 s.
- Tipp: exakte Suche (Bitboard-DFS mit explizitem Stapel, Symmetrie-Kanonisierung, offene Hash-Tabelle 2^24 Float64 = 134 MB). Auf dem Englischen Brett endet „ohne Limit" immer mit Beweis (Raum 23.475.688 Stellungen). Europäisch/Wiegleb: Suche bis Fund oder Abbruch.
- **Hilfe in zwei Schaltern** (Menü › Hilfe, v1.54): **Computer** – der Rechner bewertet jeden Zug: Ampel, Warnblitz + Alarmton, Erklärung hinter dem Pfeil (Fehlerklassen Reihenfolge / Falsche Richtung / Stein gestrandet / Struktur mit Suchgegend), Markierung gestrandeter Steine (`optFinal`, nur ≤ 24 Steine), probierte Züge als rote Dreiecke. Aus = wie am echten Brett, nur Vor und Zurück. **Tipps** – Hilfe auf Anfrage, gezählt: Tipp-Knopf und „Fehler suchen". Chip unter den Zählern schaltet Computer.
- Lektionen (Menü › Lernen): Dreier-, Sechser-, Neuner-Purge, die fünf Endfelder, Partie als Purge-Folge (`plan.json`: 2× Dreier, 3× Sechser, Endspiel 7 Züge).

## Architektur in einer Datei
- `coreFactory()` – Bretter, Züge, Symmetrien, 3-Färbungs-Parität (`parityLowerBound`, `allowedEndCells`), `createTable`, `createDFS`, `createSearch` (Neustart-Portfolio + erschöpfende Phase, `full:true` = ohne Symmetrie mit `everEmpty`/`optFinal`), `solveSmart`. Derselbe Code läuft im Web Worker (Blob-URL); scheitert der Worker (Claude-Vorschau, `data:`-URL), fällt alles auf den Hauptthread in Häppchen zurück (`runSearch`).
- Zustand `game` (pegAt mit Stein-IDs, history/future mit `jumped`), `settings` in localStorage (fehlt in `data:`-Kontexten – abgefangen).
- Rendering: SVG, Overlay-Layer für Auswahl/Tipp/Spul-Markierung/Lektionsringe; Brettgröße aus `fitStage` (nutzbare Höhe ohne Schutzränder). `ensureStatusFits()` misst nach jeder Meldung im Gerät nach und gibt Brettfläche nur ab, wenn die Zeile sonst hinter der Fußleiste läge – nur nach unten, damit das Brett im Spiel nicht springt.

## Gesicherte Fakten (nicht neu diskutieren)
- Europäisch mit leerer Mitte ist nie auf 1 lösbar (Parität); Raute (41) hat keine gefundene Lösung → weggelassen.
- „L-Purge" mit zurückkehrendem Hilfsstein existiert nicht (alle Varianten mit 1–2 Katalysatoren durchprobiert).
- Zählung der Suche = verschiedene Stellungen (Spiegelungen/Drehungen zusammengefasst), nicht Besuche. iPhone: ≈0,4–0,5 Mio. Stellungen/s.
- Erreichbare Stellungen zweier Beispielstellungen mit 27 Steinen: 5.717.512 und 5.965.349 (exakt, BFS = DFS).
- Gelbe Ampel = die Bewertung war nach 6 s (`timeMs`) nicht vollständig. Gemessen über je drei Zufallsstellungen mit 23–32 Steinen: **alle** liefen durch, die längste in 5,0 s. Gelb ist also der Ausnahmefall, nicht der Eroeffnungszustand. Die früheren Nutzertexte „ab etwa 26 Steinen wird bewertet“ waren falsch und sind ersetzt.
- **Lutz’ Stellung vom 13.09.2026, 13:57 Uhr** (Screenshot, Englisch, 14 Steine): bestenfalls 2 Steine in **2 Endbildern**; in beiden bleibt genau **(4,6)** besetzt – der Stein mit dem ⊗ im Screenshot. Unabhängig nachgerechnet und als Testfall hinterlegt.
- **Lutz’ Stellung vom 13.09.2026** (Screenshot, Englisch, 23 Steine; im Test als Bild hinterlegt): vorher 1 Stein erreichbar, nach grün (4,6) über (4,5) nach (4,4) bestenfalls 2 mit **8 Endbildern** – unabhängig nachgerechnet, die 8 stimmen mit seinem Screenshot überein. Der bessere Zug füllt dasselbe Loch von oben: (2,4) über (3,4) nach (4,4). Kein Stein strandet, deshalb fiel es früher in die Restklasse „Struktur“.
- Die Zahl **24** (`full = count<=24`) steuert etwas anderes: nur darunter werden zusätzlich `everEmpty`/`optFinal` berechnet, also die Daten für die Markierung. Sie hängt nicht an `settings.marks` – bekannt, bewusst so gelassen.

## Konventionen von Lutz
- Eine ausgelieferte Datei: `index.html`. Version in `APP_VERSION` und im Menü. Keine versionierte Zweitkopie im Repo – Git hält die Stände. **Keine Git-Tags** (Entscheidung von Lutz, 13.09.2026): Jede Auslieferung trägt die Versionsnummer schon in ihrer Commit-Meldung, damit ist jeder Stand wiederzufinden – ein Tag wäre ein zweites Etikett auf derselben Kiste. Dazu kommt, dass Cloud-Sitzungen Tag-Refs nicht schreiben dürfen (reproduzierbar HTTP 403, Branch-Push geht), die Regel also bei jeder Auslieferung eine Handarbeit für Lutz erzeugt hätte. Der einzige vorhandene Tag `v1.1` liegt nur lokal und ist nie auf GitHub angekommen.
- **Lutz benachrichtigen, wenn eine Arbeit fertig ist – IMMER** (Wunsch vom 13.09.2026: „heute dauert es alles relativ lange“; **verschärft am 15.09.2026**: „kannst du mir ab jetzt immer wieder eine Info-Nachricht aufs Handy schicken, wenn du die Aufgabe beendet hast“). Rechnungen, Testläufe und Pages-Builds laufen hier minutenlang; Lutz legt das Gerät solange weg und weiß sonst nicht, ob noch etwas läuft.
  Also **am Ende jedes Arbeitsschritts** eine Push-Nachricht – kurz, eine Zeile, mit dem Ergebnis voran („v1.23 ist live“, „drei Prüfungen rot“, „nichts läuft mehr“), nicht „fertig“. Auch dann, wenn **keine Auslieferung** dabei war und auch dann, wenn Lutz **gerade mitliest**: die frühere Einschränkung („nicht, wenn er offensichtlich mitliest“) ist damit **aufgehoben** – er kann am Bildschirm nicht erkennen, ob noch eine Hintergrundaufgabe läuft, und genau das war der Anlass. Weiterhin **keine** Nachricht bei jeder Zwischenantwort innerhalb eines Arbeitsschritts – gemeint ist der Abschluss, nicht jeder Tool-Aufruf.
  **Hintergrundaufgaben gehören zum Arbeitsschritt.** Ein Warte-Befehl auf den Pages-Build erscheint Lutz als „1 laufende Aufgabe“, ohne dass er sieht, worauf gewartet wird (Screenshot 15.09.2026, 13:53). Also erst melden, wenn wirklich nichts mehr läuft.
- **Nach jedem Push den Pages-Build nachsehen.** Der Push allein bringt nichts auf Lutz’ iPhone – GitHub Pages muss daraus erst eine Auslieferung bauen. Das läuft automatisch, **aber nicht zuverlässig**: Am 13.09.2026 wurde der Build für v1.22 (`66e780f`, 08:47) schlicht ausgelassen – zwischen Lauf 30 (v1.21) und Lauf 31 (der nächste Commit, 09:17) existiert kein Lauf für ihn. Lutz spielte deshalb eine halbe Stunde lang v1.21 und hielt die fehlende Nummer für einen Anzeigefehler. Deshalb nach jedem Push prüfen (`actions_list`, `list_workflow_runs`), ob ein Lauf mit dem eigenen `head_sha` existiert und auf `success` steht; fehlt er, einen weiteren Commit hinterherschicken. **Am 13.09.2026 dreimal passiert** – bei v1.22 gar kein Lauf; bei v1.26 ein Lauf, der den **vorherigen** Commit baute (Lauf 37 um 17:10 mit dem `head_sha` von v1.25); bei v1.28 **zwei** Läufe (39 und 40), die beide den vorherigen Commit bauten – und der trug den Menü-Fehler, stand also kaputt auf dem Gerät. Der Aussetzer ist damit kein Einzelfall, sondern die Regel bei schnell aufeinanderfolgenden Pushes: **immer nachsehen, immer den `head_sha` vergleichen**, und bei einer Fehlerkorrektur erst recht. Der `head_sha` des Laufs ist also zu prüfen, nicht nur seine Existenz. Die Versionsnummer im Startbild ist das verlässliche Signal, was wirklich auf dem Gerät liegt – stimmt sie nicht, fehlen die Änderungen mit.
- **Trainer und Strategie-Hinweise sind standardmäßig an** – und werden einmalig eingeschaltet (`settings.anGestellt`, v1.35). Ein geänderter Standard allein erreicht niemanden, der die App schon installiert hat: `Object.assign` zieht den gespeicherten Wert vor, und Lutz trug `strategy:false` seit der ersten Installation mit sich. Die Umstellung läuft genau einmal; wer die Schalter danach abschaltet, behält das (zwei Prüfungen in `test_browser`, die zweite ist die Negativprobe). Vorher waren beide aus – wer die App frisch installierte, sah eine Ampel, die nie leuchtete, eine leere zweite Zeile und weder Lauflicht noch Warnblitz: Die Bewertung läuft nur, wenn Trainer, Strategie oder Markierung an ist. Gemessene Kosten: unter 1 s in der Eröffnung, meist unter 100 ms, im Hintergrund.
- **Antworten auf eine Berührung** („Dieser Stein kann nicht springen“ u. ä.) laufen über `hinweis()` und haben 2,2 s Vorrang vor der Bewertung – sonst überschreibt die gleichzeitig fertig werdende Bewertung des vorigen Zuges sie, bevor man sie lesen kann. Ein Zug oder ein neues Spiel beendet den Vorrang sofort (`hinweisBis=0` in `afterMove` und `newGame`), sonst verzögert er die nächste Bewertungsanzeige.
- **Markierung des letzten Zuges** (nur beim Spulen, `game.lastMove`) verschwindet, sobald man das Brett berührt (`game.markAus`, gesetzt in `onTap` **und** im `pointerdown` des Bretts, damit auch ein Tipp neben die Felder aufräumt) – sie stand sonst beim Nachdenken im Weg. Beim nächsten Spulen erscheint sie wieder. Fünf Prüfungen in `test_browser`.
- **Der Warnblitz vergleicht gegen die Vorgängerstellung, nicht gegen die neue Schranke** (`pruefeVerlust`, `game.prevPos`, `evalCache`, `prevEval.lb` – v1.35). Zwei Fehler steckten darin, beide am 13.09.2026 gemessen:
  (1) Die Bedingung lautete `best > lb`, wobei `lb` die Paritätsschranke der **neuen** Stellung war. Beim Solitär steigt diese Schranke oft durch den Zug selbst mit – gemessen am englischen Brett nach 14 Zügen: `best` ging von 1 auf 2, `lb` der neuen Stellung war ebenfalls 2, also war `best > lb` falsch und **kein Blitz**, obwohl die Lösung genau in diesem Zug verloren ging. Richtig ist der Vergleich gegen die Bewertung **und** die Schranke der Vorgängerstellung: Alarm bei `prev.best <= prev.lb && ev.best > prev.best`. `prevEval` trägt sein `lb` deshalb selbst.
  (2) `prevEval` war `null`, wenn beim Zug weder eine fertige Bewertung noch eine bekannte Linie vorlag – also mitten in der Partie der Normalfall, sobald Lutz zügiger spielt als gerechnet wird. Zwei Gegenmittel: jede fertige Bewertung bleibt unter ihrem Stellungsschlüssel in `evalCache` liegen (geleert bei `newGame`), und reicht auch das nicht, merkt `applyMove` die Belegung vor dem Zug in `game.prevPos`; `pruefeVerlust()` rechnet sie **nach**, sobald feststeht, dass überhaupt etwas verloren gegangen sein kann. Der Filter dafür ist gratis: der Alarm verlangt `ev.best > prevPos.lb`, sonst wird nicht nachgerechnet. Gemessen: 804 ms für die Nachrechnung auf dem englischen Brett mit 19 Steinen.
  Damit zählt auch der Rettungszähler wieder – er hängt über `game.alarmZuege` am Alarm. **Fünf neue Prüfungen in `test_browser`** (Blitz bei zügigem Spiel, Blitz obwohl die Schranke mitsteigt, Rettung danach, Blitz über die Nachrechnung, Gegenprobe mit einem guten Zug).
- **Warnblitz** (`settings.alarm`, Standard **an**, Menüzeile Warnblitz): Kostet ein Zug die Lösung, zieht einmal ein rotes Leuchten über den ganzen Bildschirm (`#warnblitz`, 0,62 s, `pointer-events:none`) und dazu `Sound.alarm()` – der gewählte Alarmton (siehe eigener Punkt), bewusst anders als `Sound.bad()` (kurzer Stups bei einem unmöglichen Zug) und `Sound.lose()` (Ende der Partie). Ausgelöst nur beim **Übergang** – vorher war das Paritätsminimum erreichbar, jetzt nicht mehr; die Prüfung sitzt in `finish()` der Bewertung und vergleicht gegen `game.prevEval`, **nicht** in `setStatus` (rote Meldungen wie „Dieser Stein kann nicht springen“ dürfen nicht flackern). Kein zweiter Blitz in einer schon verlorenen Stellung. Vier Prüfungen in `test_browser`.
- **Fertig steht fest unten** (`.fuss` in `#sheet` und `#detail`): Beide Blätter sind länger als jeder Bildschirm – Lutz musste jedes Mal ans Ende scrollen, nur um das Menü zu schließen. Der Knopf sitzt jetzt in einer eigenen Fußzeile **hinter** dem Scrollbereich, mit Trennlinie und Schutzrand. **83 px hoch, davon 34 px Schutzrand** (Home-Leiste des iPhones, Vorgabe) – gestaltbar sind also nur 49 px: 6 px Polster oben, 40 px Knopf, 2 px unten. Der Knopf ist 40 statt 44 px hoch: Apples 44 pt gelten für die kleinste Kante eines **freistehenden** Ziels, hier geht er über die volle Breite und hat 14.400 px² Trefferfläche statt der geforderten 1.936. **`.scroll` braucht `flex:1 1 auto` und `min-height:0`** – beide Fallstricke einmal erlebt: ohne `min-height:0` schrumpft ein Flex-Kind nicht unter seine Inhaltshöhe und drückt die Fußzeile aus dem Blatt; mit `flex-basis:0` passiert das Gegenteil – das Blatt hat nur `max-height`, keine feste Höhe, also bestimmt der Inhalt sie, und der ist mit Basis 0 fast nichts. **v1.27 ging mit genau diesem Fehler live**: das Menü schrumpfte auf 134 px mit 16 px sichtbarem Inhalt. Die Layout-Prüfung ließ es durch, weil sie nur fragte, ob der Knopf im Bild liegt (ja) und ob gescrollt werden kann (ja) – beides stimmte. Gefunden hat es **Lutz durch Nachfragen**, nicht der Test. Seither prüft `test_layout` die Blatthöhe selbst gegen das Maximum und verlangt, dass der Inhalt mindestens 70 % des Blattes bekommt; Negativprobe gemacht. „Bestleistungen löschen“ bleibt bewusst im Scrollbereich: selten gebraucht, und neben „Fertig“ fest unten wäre die Verwechslungsgefahr größer als der Nutzen. Achtzehn Prüfungen in `test_layout` über drei Gerätehöhen (im Bild, groß genug, bleibt beim Scrollen stehen, Inhalt darüber scrollt weiter, Blatt nutzt die volle Höhe, Inhalt bekommt ≥ 70 %). **Beim Messen daran denken, dass das Blatt 0,3 s hochfährt** – vorher gemessen liegt alles noch unterhalb des Bildschirms.
- **Der Schutzrand im Sockel wird gemessen, nicht angenommen** (`sockelRandSetzen`, `--sockelrand`): Lutz’ Screenshot vom 13.09.2026, 20:39 Uhr, pixelweise vermessen – Knopf endet bei 757 pt, das **Blatt** bei 793 pt, der Bildschirm bei 852 pt. Zwischen Knopf und Blattunterkante lagen genau die vorgesehenen 36 pt; die **59 pt darunter gehören nicht zum Blatt**. So etwas passiert, wenn die Seite in Safari mit Leiste läuft: der Viewport ist kürzer als der Bildschirm, und der Streifen darunter hält den Abstand zur Home-Leiste schon von allein – der eigene Schutzrand ist dann doppelt. `sockelRandSetzen()` misst `screen.height - window.innerHeight` und legt nur drauf, was davon fehlt (mindestens 2 px). Läuft die App vom Home-Bildschirm (Viewport = Bildschirm), bleibt der volle Schutzrand. **Am Gerät bestätigt** (Screenshot 21:01 Uhr): der Knopf endet jetzt bei 791 statt 757 pt, also 2 pt über der Blattunterkante – 34 pt tiefer, der Sockel von 83 auf 49 px. Lutz ruft die App also **in Safari** auf, nicht vom Home-Bildschirm. Die Messung bleibt trotzdem eine Heuristik: die Differenz kann auch von einem verkleinerten Fenster stammen. Auf dem Desktop ist `--sab` ohnehin 0, dort ändert sie nichts. Drei Prüfungen in `test_layout`; die Fertig-Knopf-Prüfungen stellen dort `screen.height = window.innerHeight`, sonst hält die Messung die Differenz zum Testmonitor für freien Platz.
- **Zwei Zähler für die Partie** (`game.rueckAlarm`, `game.tippKeys`, `game.alarmZuege`, `zaehlerZahlen`/`zaehlerLang`): **Rettungen** zählen **nicht jedes Zurück**, sondern nur das Zurücknehmen genau des Zuges, der die Lösung gekostet hat – `warnblitz()` merkt sich dafür die Zuglänge in `alarmZuege`, `undo()` streicht sie beim Zählen wieder heraus, damit ein wiederholter Zug nicht doppelt zählt. Die Zuglänge wird auch gemerkt, wenn der Warnblitz **aus** ist – gezählt wird der verlorene Zug, nicht das Aufleuchten; nur die Probe aus dem Menü (`erzwingen`) zählt nicht. **Tipps** zählen je Stellung einmal (`tippKeys` als Menge von `stateKey()`): denselben Tipp zweimal ansehen ist ein Tipp.
  **„Rettung“, nicht „Rücknahme“** (Wortwahl von Lutz): der Knopf heißt Zurück, aber man drückt ihn, um sich zu retten.
  Angezeigt **rechts in der Zählerzeile als Block aus vier Zellen** (`dl.neben`): Wort und Zahl **nebeneinander**, zwei Zeilen untereinander – nicht als zwei weitere Spalten mit Zahl über Wort, das war mein erster Versuch und nicht gemeint. Die Zahlenspalte hat eine **feste** Breite (`2.1ch`): mit `auto` wird der Block breiter, sobald die Zahl zweistellig wird, und verschiebt die drei Hauptspalten. Beschriftung **„Rettung“ im Einzahl-Sinn** als Kategoriename – „Rettungen“ machte den Block breiter als eine Hauptspalte (89 gegen 79 px gemessen), und dann zieht er die Aufmerksamkeit auf sich, die den großen Zahlen gehört. Bei null ein **Strich statt einer Null**; ab eins in Messing hervorgehoben. Unter 386 px Bildschirmbreite werden alle Beschriftungen kleiner, sonst wird „Übrig von 32“ zweizeilig. Gemessen: Block 75 px gegen 84 px Hauptspalte (iPhone 14 Pro). Im **Ergebnis** stehen sie seit v1.36 als eigene Zahlen neben der Steinzahl (eigener Punkt). Dreizehn Prüfungen in `test_browser`, einundzwanzig in `test_layout`.
- **Im Ergebnis stehen die Zähler neben der Steinzahl, nicht im Satz** (`.ergzeile`, `.ergneben`, `resRett`/`resTipp` – v1.36): Bis v1.35 lasen sie sich als „Dabei 8 Rettungen und 1 Tipp." mitten im Fließtext und gingen dort unter – Lutz am 14.09.2026: „sehr wichtige Leistungsmerkmale". Jetzt links die große Steinzahl (56 px), rechts daneben die beiden Zahlen (30 px, in Messing ab eins), beide auf einer Höhe; unter 330 px Kartenbreite bricht die Zeile um. Der Satz nennt sie nicht mehr – eine Prüfung wacht darüber, dass sie sich nicht wieder hineinschleichen. **Anders als in der Zählerzeile zeigt das Ergebnis bei null eine echte Null**: am Ende einer Partie ist „0 Rettungen" die Leistung, über die man sich freut; live wäre sie nur „noch nichts passiert" und steht dort weiter als Strich. Ganze Worte in der Mehrzahl („Rettungen", „Tipps") – dafür ist im Blatt Platz, in der Zählerzeile nicht. `zaehlerLang()` ist damit weggefallen. Fünf Prüfungen in `test_browser`, sechs in `test_layout`.
- **Die Zeit hält die Zeit an** (`pauseSetzen`, `game.pauseAn`, `#pause` – v1.36): Ein sechster Knopf in der Fußleiste hätte jeden von 78 auf 65 px gedrückt; die Uhr ist der Gegenstand, sie anzutippen ist die naheliegende Geste. Trefferfläche gemessen 83 × 49 px, weit über Apples 44 pt. Während der Pause steht „Angehalten" statt „Zeit".
  **Das reichte nicht** (v1.38): Der einzige sichtbare Hinweis war ein Pausenzeichen von **7 × 9 px in Grau** neben dem grauen Wort „Zeit" – Lutz am 14.09.2026: „so klein, dass man ihn kaum sieht bzw findet". Die Trefferfläche war gemessen und richtig, nur nützt sie nichts, wenn niemand weiß, dass dort ein Knopf ist; die Messung beantwortete die falsche Frage. Die Pause steht jetzt als **Chip neben den Strategie-Hinweisen** (`#btnPause`, 83 × 29 px = 2407 px² gegen 63 px² vorher), beschriftet „❙❙ Pause" bzw. während der Pause „▶ Weiter" in Messing. Gemessene Gründe gegen die Alternativen: in der **Fußleiste** wären aus fünf Knöpfen zu 68 px sechs zu 56 px geworden, und die **Zählerzeile** hätte ihre Spalten verschoben; in der Chip-Zeile lagen dagegen **191 px frei**, und die Chip-Form kennt das Auge dort schon als etwas Drückbares. Beide Chips sind 29 px hoch – unter Apples 44 pt, aber das gilt für den Strategie-Chip seit jeher und hat nie gestört; die Chip-Zeile wächst dadurch nicht und kostet keine Brettfläche. Ohne laufende Uhr (vor dem ersten Zug, nach dem Spielende) ist der Chip matt, sonst führte er ins Leere. Die Uhr bleibt zusätzlich antippbar, jetzt als stille Abkürzung; das winzige Zeichen daneben ist weggefallen. Balken und Dreieck sind CSS bzw. SVG, **kein Schriftzeichen** – iOS rendert ⏸ und ▶ als blaue Emoji (dieselbe Falle wie beim ℹ in der Statuszeile). **Das Brett wird dabei verdeckt** (`#pause`, deckend mit `var(--bg)`, `position:absolute` über `#stage`): eine Pause mit sichtbarem Brett wäre keine Pause, sondern ein Zeitstopp zum Nachdenken – und dann ist jede Bestleistung wertlos. Die Fußleiste ist mitgesperrt (Zurück, Vor, Tipp), sonst wirkten sie durch die Decke hindurch. `game.pauseAn` ist bewusst etwas **anderes** als `game.paused` (Wegschalten der App): eine von Hand gesetzte Pause überlebt die Rückkehr aus dem Hintergrund, sonst liefe die Uhr wieder, ohne dass Lutz es wollte. Der Weiter-Knopf ist ein **Quadrat von 104 px genau in der Mitte** der Brettfläche (Wunsch von Lutz, 14.09.2026: als Zeile quer über das Brett las er ihn nicht als Knopf). Dafür ist `#pause` ein Grid mit den Reihen `1fr auto 1fr`; die Reihen sind den drei Teilen **ausdrücklich zugewiesen** (`grid-row`), weil das Grid sonst nach der Reihenfolge im Text sortiert – dort steht die Erklärung vor dem Knopf, und der saß damit 114 px zu tief (gemessen). Die Textreihenfolge bleibt Titel – Erklärung – Knopf, damit sie vorgelesen einen Sinn ergibt. Eigene Klasse `.pknopf` statt `.btn`: der trägt `flex:1` für die Knopfreihen der Blätter und streckte sich über die ganze Brettfläche (einmal erlebt). Das Wiedergabe-Dreieck ist ein **eigenes SVG** – ▶ rendert iOS als blaues Emoji, dieselbe Falle wie beim ℹ. **Beim Pausieren verschiebt sich nichts**: „Angehalten" ist breiter als „Zeit ❙❙", die drei Hauptspalten sind aber `1fr` und halten still; Zeilenhöhe, Statuszeile und Brettbreite bleiben auf den Pixel gleich (drei Prüfungen, vorher nur gehofft). Vierzehn Prüfungen in `test_browser`, zwölf in `test_layout`.
- **Ein neues Spiel verwirft laufende Animationen** (`abortAnimation`, v1.36): `finishAnimation()` **führt** den Abschluss einer Animation aus; `newGame` darf das nicht, denn dieser Abschluss gehört zum alten Spiel und spielte seinen Zug sonst auf dem frischen Brett nach (`applyMove` im Callback von `redo`/`undo`). Aufgedeckt hat es die Pause – sie räumt Animationen über `finishAnimation()` ab und stürzte dabei in `redo()` ab, weil `game.lastMove` durch das zwischenzeitliche `newGame` schon `null` war. Der Fehler steckte unabhängig von der Pause im Code und war nur nie sichtbar geworden. Zwei Prüfungen in `test_browser`.
- **Funkeln** (`glanzAn`, `glanzMarkup`, `settings.funkeln`, Standard **an** – v1.39, umgebaut v1.40): Ein Streiflicht wandert einmal schräg über die Murmel (950 ms). Ausgelöst wird er nach **einer Regel von Lutz** (14.09.2026): **Der Glanz gehört dem Stein, der liegen bleibt.** Also beim Antippen für einen Stein, der sich nicht bewegt – entweder weil **kein Zug möglich** ist oder weil **mehrere Ziele zur Wahl** stehen –, und ansonsten für den **gelandeten** Stein nach einem Sprung (in `playMove`, **nach** `render()`, weil `renderPegs()` die Steine neu baut und ein vorher gestarteter Glanz ins Leere liefe). Bewusst **kein Dauerfunkeln**: ein Brett, das von allein glitzert, zieht beim Nachdenken die Augen von der Stellung weg.
  **v1.39 war für einen Teil der Nutzer unsichtbar, und die Prüfungen ließen es durch** – Lutz am 14.09.2026: „Glanz ist auch angestellt aber wenn ich tippe oder springe, funktioniert es noch nicht.“ Die Fassung schob ein Rechteck von `translate(-96,0)` durch einen Ausschnitt über die Murmel. Wer **„Bewegung reduzieren“** eingeschaltet hat (auf iOS häufig), bekam den `reduce`-Zweig, und der setzte `transform` **nie** – das Rechteck blieb 96 Einheiten neben einer Murmel mit Radius 34 stehen und wurde weggeschnitten. Gemessen: **null geänderte Bildpunkte durchgehend**, gegen 294 ohne die Einstellung. Die alte Prüfung fragte, ob `opacity` gesetzt **wird** – das wurde es. Sie fragte nicht, ob man etwas **sieht**. Derselbe Fehlertyp wie die Trefferflächen-Messung bei v1.38 und die Knopf-im-Bild-Prüfung bei v1.27: richtig gemessen, falsche Frage.
  **Seit v1.40 ist der Glanz ein Kreis in Murmelgröße** (`<circle class="glanz" r="34">`), gewandert wird über die **Stops seines Verlaufs** (drei Stops, Kern wandert von −0,28 bis 1,28, Offsets geklemmt). Damit gibt es **keinen Ausschnitt** mehr – die Form *ist* die Murmel, es kann nichts über den Rand ragen und keine Stellung geben, in der nichts zu sehen ist. Nebenbei fällt der WebKit-Zweifel am `clip-path` mit `userSpaceOnUse` unter transformierten Vorfahren weg. Jeder Schritt setzt zusätzlich **`fill-opacity` auf dem Kreis selbst**: so hängt das Neuzeichnen an einem element-eigenen Attribut und nicht daran, ob die Engine eine Änderung tief im Verlauf bemerkt. Jeder Lauf bekommt einen **eigenen Verlauf** (`mglz<n>` in `defs`, am Ende wieder entfernt) – teilten sich zwei gleichzeitig funkelnde Steine einen, spränge der Streifen des einen zurück, wenn der andere beginnt. Im `reduce`-Zweig steht der Streifen **in der Mitte** (p = 0,5) und blendet nur auf und ab.
  **Bewegt wird weiterhin über SVG-Attribute, nicht über CSS-`transform`** – die allererste Fassung der Auswahlseite benutzte CSS-Transforms auf SVG-Kindern und war auf dem iPhone unsichtbar, im Testbrowser nicht. Safari behandelt das anders als Chromium.
  **Der Testbrowser ist Chromium, nicht WebKit** – und genau dieser blinde Fleck hat den Fehler zweimal durchgelassen. WebKit lässt sich hier nicht nachinstallieren (`npx playwright install webkit` scheitert an der Netzwerk-Policy, HTTP 403 auf `cdn.playwright.dev`). Also: Bei allem, was mit SVG-Rendering zu tun hat, **nicht auf den grünen Testlauf vertrauen** – Safari-Eigenheiten baulich umgehen statt sie zu testen.
  **Wer liegen bleibt, glänzt** (v1.41 → v1.42, `springtSofort` in `onTap`): In v1.39/1.40 stand `glanzAn(i)` hinter **zwei** `return`-Zweigen – ein Stein **ohne möglichen Zug** kam nie dorthin, und bei **Auto-Sprung** (Standard **an**) auch nicht. Übrig blieb der Auswahl-Zweig, den man nur bei mehreren Zielen erreicht: im Normalfall also nie. Lutz: „wenn ich einen antippe, der sich nicht bewegt … glänzt beim Antippen nicht“. v1.41 schob den Aufruf an den Anfang – und traf damit **einen Fall zu viel**: auch der Stein, der sofort wegspringt, glänzte noch an seinem Ausgangspunkt, wo er im nächsten Augenblick nicht mehr ist. Lutz stellte die Regel klar: „Nur beim Landen für Steine die springen. Auch beim Tippen für Steine die sich nicht bewegen – entweder weil es keine Sprungsteine sind oder weil mehr als eine Option besteht.“ Seit v1.42 entscheidet deshalb `const springtSofort = settings.autoJump && mv.length===1` vor allen `return`-Zweigen; `glanzAn(i)` läuft nur, wenn der Stein **nicht** sofort wegspringt. **`onSwipe` bleibt unangetastet** – ein Wisch ist kein Tippen, und Lutz' Regel spricht vom Tippen. **Vier Fälle, alle einzeln geprüft** (kein Zug / mehrere Ziele / genau ein Zug ohne Auto-Sprung / Auto-Sprung), denn v1.39 traf nur einen davon und v1.41 alle vier. Für den Fall „mehrere Ziele“ muss der Test erst ein paar Züge spielen: in der Eröffnung ist das Brett zu voll, dort hat kein Stein zwei Ziele.
  **Die Helligkeit hat ein Plateau, keine Spitze** (v1.41): `Math.sin(t*π)` steht nur einen Augenblick oben – Lutz: „leuchtet aber ein bisschen wenig und sehr kurz“. Mit `Math.min(1, Math.sin(t*π)*1.9)` bei 0,95 steht der Glanz **667 ms bei voller Kraft** statt rund 180 ms, blendet an den Rändern aber weiter weich auf und ab. Dauer von 620 auf 950 ms, Streifenbreite von 0,28 auf 0,36. **Zeit bei voller Kraft ist die Größe, die zählt** – nicht die Gesamtdauer und nicht der Spitzenwert: beide waren vorher schon in Ordnung und trotzdem war es „sehr kurz“.
  **Gemessen wird jetzt, was zu sehen ist** (`glanzBildpunkte`, pngjs als devDependency): geänderte Bildpunkte gegen das Standbild, **in beiden Bewegungs-Einstellungen**. v1.40: normal 2032, reduziert 1842, beide enden bei 0. Negativprobe gemacht – der alte `reduce`-Fehler nachgebaut macht genau diese eine Prüfung rot (1 statt 1842). Beim Messen daran denken, dass die **Auswahl-Markierung selbst animiert ist**: misst man den angetippten Stein, liegt ein Sockel von rund 1500 Bildpunkten unter dem Ergebnis (einmal erlebt). Zwanzig Prüfungen in `test_browser`.
- **Wie hell die Ampel leuchtet** (`ampelWerte`, `ampelKraftSetzen`, `settings.ampelKraft` 0–10, Standard **0** – v1.43, fließend seit v1.44): **Langer Druck (550 ms) auf die Ampel** öffnet `#ampelBlatt` mit einem Schieberegler. Wunsch von Lutz (15.09.2026): die Lichter kräftiger einstellbar, „bis hin zu einem größeren Flash auch vom Hintergrund“.
  **Drei Größen, fließend gerechnet** (`schein` / `dicht` / `hof` aus `t = n/10`). v1.43 hatte fünf feste Stufen; Lutz wollte den großen Schein im Bild auch **schwächer** haben, nicht nur an oder aus – daher elf Positionen und eine Formel statt einer Tabelle.
  **Ein wachsender Weichzeichner ist keine Verstärkung.** Der erste Entwurf drehte nur am Blur – und wurde dadurch **breiter, aber flacher**: gemessen sank die stärkste Abweichung von unten nach oben von **33 auf 23**, obwohl die Fläche wuchs. Es sah diffuser aus statt kräftiger. `dicht` (der `spread` des Schattens) hält den Kern zusammen: stärkste Abweichung jetzt 43–50 über die ganze Leiter, Fläche 2295 → 10702 geänderte Bildpunkte.
  **Der Schein im Bild ist abgestuft, nicht an/aus** (`AMPEL_HOF_AB = 3`): Stufen 0–2 lassen den Raum dunkel, ab 3 wächst er in acht Schritten von 12,5 % auf volle Deckkraft (gemessen über den ganzen Bildschirm: 299k → 1,17M → 1,97M → 3,20M Summe der Abweichung). Die Formel ist `(n − ab + 1)/(max − ab + 1)`, **mit +1 in Zähler und Nenner** – ohne das wäre die Einsatzstufe rechnerisch genau 0 und damit wirkungslos, der Schein begänne erst eine Stufe später als der Name verspricht.
  **Die unterste Stufe ist exakt der alte Zustand** (Schein 8 px, dicht 0, Hof 0, kein Schein im Bild) – wer nichts einstellt, merkt nichts.
  **Die Probe im Blatt teilt die CSS-Deklarationen mit der echten Ampel** (gemeinsame Selektorliste `#status.bad .dot b.r, … , .aprobe b.an`, Werte über `--aschein`/`--adicht`/`--ahof` auf `:root`). Zwei getrennte Regelsätze wären auseinandergelaufen; ein Test vergleicht beide berechneten `box-shadow` direkt. Gezeigt werden **alle drei Farben nebeneinander**.
  **Ziehen und Loslassen zeigen zwei verschiedene Dinge, und beide werden gebraucht** (v1.45). Beim **Ziehen** steht der Schein still (`ampelHofHalten`, Klasse `.halten`) – liefe er bei jedem Reglerschritt neu an, flackerte er über elf Positionen, und die Stärke ließe sich nicht vergleichen. Beim **Loslassen** (`change` → `ampelProbe`) läuft die **volle Probe**: die drei Proben blitzen mit denselben `blitz`-Keyframes wie das echte Ergebnislicht, und der Schein schwillt an und ab – also genau das Bild, das im Spiel beim Ergebnis entsteht. v1.44 hatte nur das Halten und damit die Bewegung verloren; Lutz am 15.09.2026: „wenn ich's loslasse, sieht man aber nicht, wie die Ampel blinkt … und auch das Aufleuchten vom Hintergrund ist jetzt weg.“ Dieselbe Probe läuft beim **Öffnen** des Blattes (nach 360 ms, sonst spielt sie hinter der hochfahrenden Kante ab) – wie beim Alarmton, wo ein Tipp die Wahl gleich abspielt.
  **Beim Einstellen liegt der Schein VOR dem Blatt** (Klasse `.vor`, `z-index:30`). Sein Zentrum sitzt bei 82 % der Höhe, das Blatt beginnt bei rund 57 % – **unter dem Blatt war er beim Einstellen vollständig verdeckt** und nie zu sehen, auch nicht in v1.43/1.44 (gemessen: Zentrum y = 692, Blattkante y = 482, `z-index` 12 gegen 22; die Animation lief korrekt mit `opacity` 0,91, nur sah sie niemand). Im Spiel bleibt er unten unter den Blättern – dort ist kein Blatt offen. Unter dem Warnblitz (45) bleibt er auch in der Vorschau, und Berührungen fängt er nicht ab. Beim Schließen wird er abgeräumt (`vor`, `an`, `halten`), sonst stünde er über dem Brett.
  **Der Schein im Bild** (`#ampelhof`) hängt am **Ergebnis-Blitz**, nicht am Setzen der Statuszeile – rote Meldungen wie „Dieser Stein kann nicht springen“ dürfen den Raum nicht aufleuchten lassen. `z-index:12` – unter den Blättern (21) und unter dem Warnblitz (45), denn er ist Beiwerk, keine Meldung.
  **Rot färbt den ganzen Bildschirm, Grün und Gelb nur den Streifen unten** (Klasse `.rot`, `ROT_STUFEN`, `ampelHofSetzen` – v1.46). Lutz am 15.09.2026: „bei Rot muss bitte der ganze Bildschirm aufflackern, bei den anderen Farben ist es so okay.“ Entscheidend ist die **Form des Verlaufs**, nicht seine Deckkraft: der rote wird nach außen **stärker** (0,30 / 0,55 / 0,75, dieselben Stützstellen wie der Warnblitz, mit der Stufe skaliert) und nutzt auch dessen Kurve (`warnblitz .62s`); der Hof von Grün und Gelb läuft nach außen aus und bleibt unten. Gemessen als mittlere Farbabweichung je Bildpunkt, oben und unten getrennt: Grün 0/10, Gelb 0/11, **Rot 35/12**, Warnblitz zum Vergleich 35/11 – Rot fühlt sich also an wie der Warnblitz, den Lutz von früher kennt. **Eine Prüfung auf die Klasse allein sähe diesen Unterschied nicht** – es wird oben und unten gemessen.
  **Unter der Einsatzstufe bleibt auch Rot aus** – die Regel „wer nichts einstellt, merkt nichts“ gilt für alle Farben; der Warnblitz kommt dort ohnehin weiter, unabhängig von dieser Einstellung.
  **Die Warnblitz-Sperre sitzt in `ampelHof()`, nicht an der Aufrufstelle** (`warnblitzGleich()`, 1200 ms): zwei rote Blitze übereinander wären keine Verstärkung, sondern Matsch. Bis v1.45 stand sie in `setStatus` – das deckte den einen Weg ab, aber nicht jeden. Aufgefallen ist es, weil die neue Prüfung `ampelHof` direkt rief und der Hof trotz laufendem Warnblitz anlief. Solange Rot ein schmaler Streifen war, fiel das nicht auf; als Vollbild schon.
  **Die Skalen-Umstellung muss hinter `AMPEL_MAX` stehen** (`settings.ampelSkala`, läuft genau einmal, rechnet 0–4 auf 0–10 um). Oben bei den `settings` platziert lag sie in der **temporalen Totzone** der `const`-Deklaration. Mit Standardwert 0 fällt das **nicht** auf – der Filter `if(settings.ampelKraft>0)` davor greift, die Zeile läuft nie, alles wirkt heil. Wer aber eine Stufe gespeichert hatte, bekam `ReferenceError: Cannot access 'AMPEL_MAX' before initialization` und **die App startete gar nicht**. Also genau für die Leute kaputt, die die Funktion benutzen. **Eine Umstellungs-Prüfung ohne wirklich gespeicherten Wert ist wertlos** – der Test setzt deshalb `solitaire.settings` (nicht `settings`, der `Store` hat das Präfix `solitaire.`; auch dieser Griff ging einmal daneben) und verlangt einen fehlerfreien Start. Negativprobe gemacht: mit der Umrechnung oben werden drei Prüfungen rot. Dazu eine Gegenprobe, dass eine schon umgestellte Einstellung nicht bei jedem Start weiter hochwandert.
  **Der lange Druck bricht bei Bewegung über 10 px ab**, sonst zöge ein Wisch übers Brett den Regler auf; `-webkit-touch-callout:none` auf der Ampel, sonst schiebt iOS beim langen Druck sein Auswahlmenü darüber. Nach einem langen Druck wird das folgende `click` verschluckt (`ampelLang`), sonst öffnete sich zusätzlich das Erklär-Blatt.
  **Beim Messen den Ausschnitt klemmen**: die Ampel steht bei x = 20, ein Rand von 26 px macht den Screenshot-Clip **negativ** – Puppeteer liefert dann stillschweigend immer dasselbe Bild, und alle Stufen messen sich gleich (einmal erlebt). Achtunddreißig Prüfungen in `test_browser`, neun in `test_layout`.
- **Probierte Züge** (`game.probiert`, `probiertMerken`, `probierteHier`, `settings.probiert`, Standard **an** – v1.47): Nimmt man einen Zug zurück, bleibt er als **kräftiger roter Strich** stehen (6 px gestrichelt in `#ff4436`, fast voll deckend) mit **kleinen Punkten an beiden Enden** (Radius 6,5). Zweck nach Lutz (15.09.2026): „dass ich sehe, welchen Zug ich schon probiert habe, wenn ich den gleichen Zug mehrfach falsch mache“.
  **Gemerkt wird je STELLUNG, nicht je Zugnummer** (`Map` von `stateKey()` auf ein `Set` von Zug-Indizes). Wer weitergeht und später an dieselbe Stelle zurückkommt, findet seine eigenen Versuche wieder vor – das ist beim Suchen nützlicher, als sie zu vergessen. Ein Zug führt in eine andere Stellung, dort ist die Tafel leer; `newGame` räumt alles ab.
  **Der Versuch wird erst im Animations-Callback eingetragen** – vorher steht die Stellung, aus der heraus gespielt wurde, noch gar nicht. Das Spulen braucht 400 ms Vorlauf + 650 ms; wer im Test früher misst, sieht nichts und hält es für einen Codefehler (einmal erlebt).
  **Gezeichnet nur, was von hier aus noch ginge** (`from` besetzt, `over` besetzt, `to` frei) – ein Strich zu einem inzwischen besetzten Feld verwirrt mehr, als er hilft.
  **Nach zwei Sekunden tritt die große Markierung zurück** und lässt nur den Strich stehen, sonst deckt sie beim Nachdenken genau das zu, was man vergleichen will. **Der Timer hängt an SEINER Markierung** (`const meine = game.lastMove`, Vergleich per Identität): ohne diese Probe räumte er auch eine Markierung ab, die inzwischen einem Vor-Zug gehört – gemessen als roter Pfeil des Vorspulens, der nach 2 s verschwand, obwohl er einen ganz anderen Zug zeigte. Eine bestehende Prüfung fand das sofort. Dreizehn Prüfungen in `test_browser`, darunter zwei auf **Pixel**, denn die Zahl der Elemente sagt nicht, ob man sie sieht.
  **Die erste Fassung war zu vorsichtig** (v1.47: `opacity .5`, 4 px, `#e0574a`) – Lutz am 15.09.2026: „man kann sie kaum erkennen“. Meine Sorge, sie könnten mit der vollen Markierung konkurrieren, war die falsche Abwägung: gegen ein Brett voller bunter Murmeln muss ein Hinweis sich behaupten, und die volle Markierung steht ohnehin nur zwei Sekunden daneben. Im direkten Vergleich derselben Stellung gemessen: **alt 178 rote Bildpunkte bei mittlerer Stärke 64, neu 413 bei 129** (stärkster Punkt 84 → 207). **Fläche allein reicht als Prüfgröße nicht** – die Striche hatten schon vorher die richtige Größe, nur eben halb durchsichtig. **Der Mittelwert taugt aber auch nicht**: er *sinkt*, sobald die Fläche wächst, weil mehr weiche Randpunkte hineinzählen (im Test 97 gegen 129 bei identischer Farbe, nur mehr Strichen) – eine Prüfung darauf wäre bei jedem zusätzlichen Strich rot geworden. Gezählt wird deshalb, wie viele Punkte **wirklich kräftig** sind (Abweichung > 100). **Negativprobe, die alles sagt**: die alte Fassung bringt im Test **800 rote Bildpunkte, davon 0 kräftige** (stärkster 97). Eine Flächenprüfung hätte sie also anstandslos durchgelassen – genau den Zustand, den Lutz „kann man kaum erkennen“ nannte. Die neue Fassung: 1521 Punkte, davon **488 kräftige**, stärkster 209. Der gestrichelte Verlauf hält sie trotzdem von der durchgezogenen vollen Markierung unterscheidbar.
- **Zwei Hauptschalter statt vier Einzelschaltern** (`settings.computer`, `settings.tipps`, `computerSetzen`, `renderComputerBtn`, `#btnComputer`, Menüabschnitt **Hilfe** mit `.row.sub[data-sub=computer]` – v1.54). Lutz am 17.09.2026: alle Hilfen kategorisieren, Namen prüfen, und „wenn man alles ausschaltet … wie wenn man spielt ohne Computer komplett ohne Hilfe".
  **Befund vorher**: Trainer, Strategie-Hinweise, Markierung und Warnblitz starteten alle denselben Rechner und zeigten nur verschiedene Ausschnitte seines Ergebnisses. Trainer und Strategie unterschieden sich seit v1.51 nur im Präfix „Trainer:". **Der Blitz-Fall von Lutz reproduzierte nicht wie beschrieben** – mit allen dreien aus kam kein Blitz (gemessen). Er kommt, wenn **Markierung** an ist und die beiden Text-Schalter aus: der Rechner läuft, blitzt, die Zeile bleibt leer. Konstruktionsfehler: der Warnblitz hing am Rechner, hatte aber einen Schalter, der so tat, als wäre er unabhängig.
  **Computer** (Standard an) bündelt Ampel, Warnblitz + Alarmton, Erklärung, Markierung, probierte Züge. Die Unterpunkte (Warnblitz, Alarmton, Markierung, Probierte Züge) stehen **eingerückt darunter und verschwinden, wenn Computer aus ist** – sonst dreht jemand am Warnblitz und wundert sich. Aus: `#status.ohneAmpel .dot{display:none}`, `evaluatePosition` kehrt um, `probierteHier` liefert null, `istGestrandet` false. `computerSetzen(false)` räumt sofort auf (Bewertung, Dreiecke, Statuszeile), nicht erst beim nächsten Zug.
  **Tipps** (Standard an): `btnHint.hidden`, `requestHint` und `findMistake` kehren um, der Link „Fehler suchen" wird nicht angeboten.
  **Zurück und Vor bleiben immer** – Entscheidung von Lutz: „gesetzt ist gesetzt" hätte zu viele Querverweise (die Erklärung bietet den Zurück-Knopf an), und ohne Bewertung ist Zurück fast wertlos – „fast": am Partieende kennt man die Steinzahl und kann anders weiterspielen; bewusst hingenommen.
  **Namensweg**: „Mitdenken" verworfen (wer denkt – der Spieler denkt auch mit), „Rechner" → **„Computer"** (Lutz). Spielarten Lernen / Üben / Ernst verworfen: Lernen und Üben sind im Deutschen nicht trennbar, „Ernst" und „Am Brett" versteht niemand ohne Erklärung – und mit zwei Schaltern braucht es keine Voreinstellungen mehr.
  **`showTrainer` hat nur noch einen Pfad** im Wortlaut der Strategie-Hinweise (von Lutz am 16.09. gelobt). `renderStrategyBtn`, `swTrainer` und alle `settings.trainer`/`settings.strategy`-Abfragen sind weg; die Schlüssel bleiben gespeichert, weil die einmalige Umstellung `hilfeSortiert` sie liest: `computer = trainer||strategy||marks` – wer alles aus hatte, behält aus.
  Siebzehn neue Prüfungen in `test_browser` (Computer aus: Ampel weg, kein Blitz, keine Bewertung, kein Dreieck, Zurück geht; wieder an ohne Neustart; Tipps aus: Knopf weg, Aufruf zählt nichts; Menü: Abschnitt Hilfe, Unterpunkte kommen und gehen; Umstellung drei Fälle). Drei alte Muster angepasst („gekostet" → auch „Strategischer Fehler", „noch" → „weiterhin").
  **Offen** [Vermutung von mir]: im Ergebnis „ohne Computer" vermerken, damit eine Partie ohne Hilfe von einer fehlerfreien mit Hilfe unterscheidbar bleibt. Lutz hat nicht entschieden.
- **Der Knopf im Blatt nimmt nur zurück – Tipps kommen ausschließlich über den Tipp-Knopf** (`rewindTo(k)` ohne `path`, `game.showBetter` **entfallen** – v1.53). Lutz am 16.09.2026: „eigentlich würde ich hier nur anbieten Zug zurück, dann kommen wir wieder aufs Spielfeld, der eben gezogene Zug ist noch mit dem roten Dreieck markiert und wir versuchen selber weiter. Den Tipp machen wir nicht durch diesen Button, sondern nur durch den Tipp-Button – dann weiß man auch, wann man einen Tipp wirklich verbraucht."
  **Aus drei Knöpfen wird einer**: „Zurück & Zug zeigen" (Trainer und Strategie) und „Dorthin zurück & Zug zeigen" (Fehlersuche) heißen jetzt **„Zug zurück"** bzw. **„Dorthin zurück"** und markieren nichts mehr. Damit ist **die ganze Fehlersuche kostenlos** – Suchen, Zurückspulen, alles. `tippZaehlen()` steht nur noch in `requestHint()`.
  **Es geht dabei nichts verloren, und das ist nachgeprüft, nicht behauptet**: `adv.path` stammt aus der Bewertung **der Stellung vor dem Fehlzug**. Nach dem Zurücknehmen steht man genau dort, der Tipp-Knopf liefert also denselben Zug – nur zählt er jetzt. Eine Prüfung geht diesen Weg wirklich: zurück, dann `requestHint()`, und verlangt Pfeil **und** einen erhöhten Zähler.
  **`showBetter` ist ersatzlos weg.** Es war eine Merkzelle, die einen Zug über die Animation des `undo()` hinüberrettete, um ihn danach zu markieren – ohne Markierung braucht sie niemand. Ein toter Zustand, der überlebt, wird irgendwann versehentlich wieder gesetzt.
  **Was stattdessen auf dem Brett steht**, ist das rote Richtungsdreieck des eigenen Versuchs (v1.52) – es kommt von `probiertMerken` und war schon da. Eine Prüfung wacht darüber, dass es nach dem Zurücknehmen wirklich steht; ohne das wäre der Knopf ein Rückschritt statt eines Umbaus. Acht Prüfungen in `test_browser`, drei davon umgekehrt.
- **Der Pfeil rechts sagt, WO zu suchen ist – nie WELCHEN Zug** (`suchIdee`, `#detailSuche`, `setSuche` – v1.51). Lutz am 16.09.2026: „Wichtig wäre, dass ich verstehe, warum dieser Zug schlecht ist … alles, was mir als Infos dient beim Denken, aber kein exakter Zug als Vorgabe." Im Blatt hinter dem Pfeil steht jetzt eine dritte Zeile, abgesetzt mit Messing-Kante: die **Suchgegend**. Sie nennt höchstens eine **Region** (`regionWord` fasst 6–9 Felder zusammen) oder eine **Menge von Kandidaten** („seine anderen Nachbarn", „seine anderen Sprünge") – nie Feld, Farbe und Richtung zusammen, denn das **wäre** der Zug. Zwei Prüfungen messen genau das: kein Farbname, keine Richtungsangabe im Text.
  **Zwei Texte mussten dafür ihre Richtung abgeben.** (1) Die Klasse „Falsche Richtung / dasselbe Loch" sagte bis v1.50 `'… musste '+dirWord(better)+' gehen, nicht '+dirWord(played)` – zusammen mit dem Loch ist das der vollständige Zug. Jetzt nur noch „es musste von einer anderen Seite gefüllt werden". **Beim Umbau einmal falsch gerechnet**: ein Zug *nach unten* kommt *von oben*, die Seite ist die Gegenrichtung – der erste Entwurf hätte sie verkehrt benannt. Deshalb gar keine Seite mehr, statt einer Formel, die man nachrechnen muss. (2) `findMistake` nannte „Besser war: …" beim Namen.
  **Damit ist die offene Entscheidung zur Fehlersuche erledigt – gekürzt, nicht gelöscht.** Sie nennt Zugnummer und Fehlerklasse, dazu die Suchgegend. Weil sie keinen Zug mehr ausspricht, **kostet das Suchen selbst keinen Tipp mehr**; `tippZaehlen()` sitzt jetzt im Knopf „Dorthin zurück & Zug zeigen", wie bei „Zurück & Zug zeigen" schon vorher. **Mein erster Vorschlag (CLAUDE.md, 16.09.) griff zu kurz**: nur den Text zu kürzen hätte nichts gekostet und nichts gebracht, denn `rewindTo` sagt selbst „Der bessere Zug ist markiert: …" – der Tipp wandert dann eine Berührung weiter, nicht weg. Der Zähler muss am **Knopf** hängen, nicht am Text.
  **Die Suchidee geht über einen eigenen Kanal** (`statusSuche`, von `setStatus` **verbraucht**): sie gilt genau für die nächste Meldung, sonst bliebe sie an der übernächsten hängen. In der **Statuszeile steht sie nicht** – die ist hart einzeilig und hätte sie ohnehin abgeschnitten. Neun Prüfungen in `test_browser`, darunter zwei umgekehrte (die alte „Erklärung nennt beide Richtungen" bewacht jetzt das Gegenteil).
- **Der Strich läuft bis IN die Punkte hinein** (`R_PENDE` = 9 – v1.50). Bis v1.49 begann er bei `R_HOLE+6` = 31 Einheiten vom Startzentrum und endete bei `R_PEG+6` = 40 vor dem Ziel, die Punkte saßen aber in den **Feldmitten** mit Radius 6,5 – dazwischen klafften **24,5 bzw. 33,5 Einheiten Nichts**. Lutz am 16.09.2026: „Der rote ist nicht durchgängig bis zum Strich, die Kreise könnten ein bisschen größer.“ Jetzt eine einzige Konstante für beides: Punktradius **und** Strichende, damit der Spalt gar nicht erst entstehen kann. Gemessen in dem Streifen, der vorher leer war (Einheiten 9–31 am Start, 160–191 am Ziel): **vorher 6 von 31 bzw. 0 von 31 Proben rot, jetzt 31 von 31 bzw. 30 von 31.** Der Rest der 6 kam nur vom weichen Rand des Punktes. **Die Zahl der Elemente sagt darüber nichts** – Strich und Punkte waren beide da, und trotzdem lag zwischen ihnen nichts; geprüft wird deshalb der **Spalt aus der Geometrie** (`Abstand Strichende zum Punktmittelpunkt minus Radius ≤ 0`) **und** die Farbe im Streifen. Negativprobe: die alte Fassung macht fünf Prüfungen rot. Radius 9 gegen das leere Loch mit 25 sind 13 % der Lochfläche – der Spielstand bleibt darunter lesbar.
- **Ein Richtungsdreieck statt eines Strichs mit zwei Punkten** (`P_BASIS` = `R_PEG/2` = 17, `.probiert` jetzt gefüllt – v1.52). **Entwurf von Lutz** (16.09.2026, mit eigenem Bild): „ein rotes Dreieck halb so groß wie der Spielstein und dann Dreieck bis in die Spitze. Dann sieht man von wo man nach wo man gesprungen ist – und nicht als Pfeil, sondern nur so quasi als Richtungsdreieck." Basis quer über dem Ausgangsfeld (34 Einheiten = halbe Murmelbreite), Spitze genau im Zielfeld.
  **Warum das besser ist als der Strich mit zwei gleichen Punkten** (v1.49–v1.51): Zwei gleiche Enden sagen **wo**, aber nicht **wohin**. Die Verjüngung sagt beides – ohne Pfeilkopf, und das ist die Bedingung: die volle Markierung des letzten Zuges **ist** ein Pfeil, zwei Pfeilarten nebeneinander wären nicht zu trennen.
  **Die Fläche wächst auf das 2,1-Fache, nicht auf das Fünffache.** Gemessen an derselben Stellung: **1.267 rote Bildpunkte vorher, 2.697 jetzt**; kräftige Punkte 752 → 2.107, stärkster 209 → 210. Meine Vorabschätzung „fünfmal so viel Rot" war falsch – sie rechnete das Dreieck gegen eine dünne Linie, der Strich war aber schon 6 px breit und gestrichelt. **Vor dem Einwand rechnen, nicht danach.**
  **Am Zielfeld verdeckt es weniger als alles davor**: die Spitze läuft auf null zusammen, das leere Loch bleibt lesbar. Genau daran war der Ring aus v1.48 gescheitert („sehr dominant, wo man nicht hin springen darf").
  **Gemessen wird die Richtung, nicht die Form.** Ein Dreieck, dessen Richtung nur im SVG steht, wäre derselbe Fehlertyp wie der unsichtbare Glanz in v1.39: richtig gemessen, falsche Frage. Die Prüfung legt deshalb **zwei Querschnitte quer zur Zugrichtung** und zählt rote Bildpunkte – bei 18 % der Strecke und bei 82 %: **14 von 41 gegen 4 von 41.** Beim alten Strich wären beide gleich gewesen, die Prüfung also die Negativprobe auf die Vorgängerfassung. Dazu fünf Prüfungen auf die Geometrie (drei Ecken, geschlossen, Basismitte im Ausgangsfeld, Spitze im Zielfeld, Basis senkrecht zur Zugrichtung – sonst zeigte das Dreieck schief) und die Basisbreite gegen `R_PEG`.
  **Offen am Gerät**: ob drei oder vier gleichzeitige Versuche noch lesbar sind. Genau dafür ist die Anzeige da, und im Test steht immer nur einer.
- **Kleine Punkte an beiden Enden statt eines Rings am Ziel** (`.probiertEnde` – v1.49). Der Ring mit `R_PEG+4` rahmte das leere Zielfeld ein, statt es lesbar zu lassen – Lutz am 16.09.2026: „sehr dominant, wo man nicht hin springen darf … so dass man den Spielstand noch sieht“. Gemessen am Zielfeld: **Ring 295 rote Bildpunkte (9,1 % des Ausschnitts), Punkte 72 (2,2 %)** – ein Viertel davon; der Strich allein liegt bei 40, der Ring war also fast der ganze Anteil. **Zwei Punkte statt einem** zeigen außerdem, **woher** der Versuch kam – das verriet der einzelne Ring am Ziel gar nicht.
- **Der bessere Zug wird nicht mehr ungefragt verraten** (`tippZaehlen()` – v1.49). Bis v1.48 stand `Besser: <Zug>` automatisch im Text von Trainer **und** Strategie-Hinweisen, ohne als Tipp zu zählen. Lutz am 16.09.2026: „Ist ja ein Tipp, der wird ja dann nicht mitgezählt … der müsste eigentlich weg.“ Damit war **die Tipp-Zahl am Ende der Partie nicht ehrlich** – eine Leistungszahl, die eine Hilfe verschweigt, ist keine. Klasse und Begründung bleiben stehen: das ist **Analyse, kein Zug**.
  **Jeder Weg, der einen konkreten Zug verrät, zählt jetzt** – der Tipp-Knopf (wie bisher), **„Zurück & Zug zeigen"** und die **Fehlersuche** (die „Besser war: …" beim Namen nennt). Alle drei rufen `tippZaehlen()`; vorher stand die Zeile nur an einer Stelle. Weiterhin **ein Tipp je Stellung** (`tippKeys` als Menge von `stateKey()`) – denselben Zug zweimal zeigen zu lassen ist ein Tipp. Zwei bestehende Prüfungen verlangten ausdrücklich das Gegenteil („nennt den besseren Zug") und sind **umgekehrt** worden: sie bewachen jetzt, dass er nicht zurückkommt. Acht Prüfungen in `test_browser`.
- **Die Ampel steht ab Werk auf Stufe 6** (`settings.ampelStd`, v1.47). Wie bei Trainer und Strategie reicht ein geänderter Standard nicht: `Object.assign` zieht den gespeicherten Wert vor. Die Umstellung läuft deshalb genau einmal und **nach** der Skalen-Umrechnung, die sie bewusst überschreibt; danach gilt, was der Nutzer wählt.
- **Das Ladefenster des Update-Hinweises wächst mit der Datei** (`UPDATE_FENSTER`, 48 kB → 80 kB): `APP_VERSION` lag bei Byte 39.865 und damit nur noch 9 kB vor der Kante – die Prüfung, die über die Lage wacht, schlug zu Recht an. Rutscht die Nummer hinaus, findet der Hinweis sie nicht mehr und meldet **still nie ein Update**; das fällt niemandem auf. Lieber 30 kB mehr laden als das. **Diese Prüfung im Auge behalten** – sie meldet sich wieder, wenn die Datei weiter wächst.
- **Die Rettung wird am ZUSTAND erkannt, nicht an der Zugnummer** (`game.imVerlust`, `game.rettungOffen`, `game.rettKeys`, `rettungPruefen()`; `game.alarmZuege` **ersatzlos weg** – v1.58). Lutz am 17.09.2026: „Ich habe zwei Schritte zurück gemacht, weil ich beim ersten Schritt nicht gesehen habe, dass ich das Ziel schon nicht mehr erreichen kann … es wird gar nichts angezeigt. Egal ob ich 12 oder drei zurückgehen muss – auf jeden Fall ein Rettungsversuch muss protokolliert werden."
  **Ursache, im Testbrowser nachgestellt**: Bis v1.57 zählte nur das Zurücknehmen genau des Zuges, dessen Zuglänge in `alarmZuege` stand. Dorthin kam sie allein über `warnblitz()` – und der setzt eine **fertige** Bewertung voraus. Wer zügig weiterspielt, bricht sie mit `cancelEval()` ab. Gemessen in zwei Durchläufen derselben Stellung: **langsam gespielt `alarmZuege=[13]`, zwei Schritte zurück → 1 Rettung; zügig gespielt `alarmZuege=[]` → 0 Rettungen.** Genau Lutz' Fall. Die Nachrechnung aus v1.35 rettet das nicht: sie vergleicht die aktuelle Stellung gegen ihre **direkte** Vorgängerin, nicht rückwirkend über einen übersprungenen Zug hinweg.
  **Lutz' Regel ist auch die robustere Definition.** Sie hängt am Zustand: `imVerlust` hält fest, dass gerade verloren steht; ein `undo()` daraus setzt `rettungOffen`; die **nächste fertige Bewertung** entscheidet – ist die Stellung wieder auf das Paritätsminimum lösbar, war es eine Rettung. Damit ist gleichgültig, wie viele Schritte es waren und ob der Warnblitz je kam. **Kein zusätzliches Rechnen**: die Bewertung nach einem Zurück läuft ohnehin.
  **Gegen Doppelzählung**: `rettKeys` sammelt die **geretteten Stellungen** – dieselbe Regel wie bei den Tipps. Wer denselben Fehler wiederholt und wieder zurückgeht, zählt nicht zweimal (gezielt gemessen: zweimal derselbe Fehlzug → 1).
  **Ohne Computer gibt es keine Rettungen** – ohne Bewertung kann niemand wissen, ob etwas gerettet wurde. Das ist konsistent: der Zähler ist eine Computer-Funktion.
  **Ein Testfehler auf dem Weg**: Ein erster Durchlauf meldete Doppelzählung, weil er `game.future[0]` für den Fehlzug hielt – der war es nach drei Undos nicht mehr. Gezielt nachgemessen zählte es richtig. **Ein Testaufbau, der die Stellung nicht selbst nachprüft, misst irgendetwas.**
  Fünf Prüfungen mit **echter** Bewertung (zügig gespielt gilt trotzdem als verloren, erster Schritt zählt noch nicht, zwei Schritte = eine Rettung, drei Schritte = auch eine, guter Zug = keine), dazu der alte Zählblock auf die neue Mechanik umgestellt. **Eine ältere Prüfung musste warten lernen**: sie rief `undo()` und las den Zähler sofort – jetzt hängt er an der Bewertung, die erst danach fertig wird.
- **Alle möglichen Züge auf Knopfdruck** (`game.zeigeZuege`, `zuegeSetzen`, `ZUEGE_MS` = 3000, `.moegl`, Chip `#btnZuege` – v1.60). Entwurf von Lutz (18.09.2026): „Kannst du einen Button machen der alle möglichen Züge anzeigt. Mein Vorschlag wären weiße Dreiecke statt rote. Da wo rot ist bleibt rot. Wenn man den Button druckt verschwindet das nach 3 Sekunden oder wenn man wieder drauf drückt."
  **Dieselbe Geometrie wie das rote Richtungsdreieck** (`P_BASIS`), nur weiß und durchscheinend (`rgba(255,255,255,.42)`). Deckend wäre es ein Teppich: bei 15 gleichzeitig möglichen Zügen liegen **5,9 % der Brettfläche** unter Weiß (gemessen); das Maximum auf dem englischen Brett waren in 40 Zufallspartien **18 Züge** bei 19–24 Steinen.
  **Ein schon probierter Zug wird gar nicht erst weiß gezeichnet**, statt weiß übermalt zu werden – „Da wo rot ist bleibt rot". Die Prüfung zählt beides: `rot === 1` und `weiß === möglich − 1`.
  **Seit v1.61 ein Dauerschalter, kein Drei-Sekunden-Blick.** Lutz am 20.09.2026: „Dauerhaft eingeblendet wäre auch mal zu probieren. Also nicht nach 3 Sekunden automatisch abschalten." Ich hatte den Dauerzustand in v1.60 ausdrücklich abgelehnt – ein Brett mit ständig fünfzehn Dreiecken verdecke genau die Stellung, die man lesen will. **Das war geschätzt, nicht gemessen**, und am Gerät entscheidet er. Der Schalter liegt deshalb in `settings`, nicht im Spielzustand: „dauerhaft" heißt, er überlebt Zug, neues Spiel **und Neustart**. Im `game` wäre er bei jedem neuen Spiel wieder aus – das Gegenteil von dauerhaft. Eine Prüfung lädt die Seite dafür wirklich neu; eine, die nur die Variable liest, hätte die Frage nicht gestellt.
  **Die Pause blendet aus, schaltet aber nicht ab** – eine Pause ist keine Entscheidung gegen die Anzeige. Der Knopf ist dabei gesperrt wie die Fußleiste, sonst wirkte er durch die Decke.
  **Drei Prüfungen sind umgekehrt worden** (nach 3 s weg → bleibt stehen; neues Spiel räumt ab → lässt an; Pause räumt ab → Schalter überlebt), dazu zwei neue (ein Zug lässt sie stehen und zeigt die neuen Züge; Einstellung ist gespeichert).
  **Ein dritter Testfehler desselben Typs, diesmal vom Dauerschalter selbst verursacht**: Die Pixelmessung nahm ihr Standbild auf, nachdem ein früherer Block den Schalter eingeschaltet hatte – bis v1.60 räumte ihn der Timer nach drei Sekunden weg, jetzt bleibt er an. Die Dreiecke steckten also schon im Standbild, gemessen **0 geänderte Bildpunkte**, und es sah aus wie ein Codefehler. **Ein Dauerzustand macht jeden Testblock abhängig vom vorigen** – wer ihn einschaltet, schaltet ihn auch wieder aus.
  **Bewusst NICHT an den Schalter Computer gebunden und bewusst kein Tipp** [meine Entscheidung, Lutz hat sie nicht getroffen]: Der Computer sagt, welcher Zug **gut** ist; dieser Knopf sagt nur, welche es **gibt**. Welche Züge die Regeln erlauben, sieht man am echten Brett mit eigenen Augen – das ist Regelwissen, keine Bewertung. `tippZaehlen()` wird deshalb nicht gerufen. Wenn Lutz das anders sieht, ist es eine Zeile.
  **Der dritte Chip sprengte die Zeile fast.** Auf 320 px Breite füllten Computer + Pause + Züge die Chip-Zeile bis auf **2 px** aus (gemessen) – kein Abstand, auf den man bauen kann, bei größerer Systemschrift wäre sie übergelaufen. Unter 386 px sind Polster und Schrift jetzt eine Stufe kleiner (dieselbe Grenze wie bei den Zählerbeschriftungen); danach **42 px frei**. Umbrechen wäre die schlechtere Lösung: eine zweite Chip-Zeile kostet Brettfläche.
  **Eigenes Chip-Zeichen `.pzt`** – lang und schmal wie das Dreieck auf dem Brett. Das breite `.pzd` des Pause-Chips liest sich als Wiedergabe-Zeichen; zwei gleiche Dreiecke nebeneinander wären nicht zu trennen.
  **Gemessen wird, was zu sehen ist**, nicht die Zahl der Elemente – derselbe Fehlertyp wie der unsichtbare Glanz in v1.39. Gemessen bei 6 Dreiecken und 20 Steinen: **16.006 hellere Bildpunkte, davon 13.148 kräftig** (stärkste Abweichung 292).
  **Zwei eigene Testfehler auf dem Weg, beide vom selben Typ: richtig gemessen, falsche Frage.** (1) Die Gegenprobe „malt nichts Rotes aufs Brett" zählte Punkte, deren **Rotkanal** gestiegen war und die rot-dominant blieben – und fand 671. Sie maß in Wahrheit die roten Murmeln: Weiß darüber macht sie heller, also steigt auch ihr Rot. Richtig ist der **Farbstich der Änderung**: Weiß hebt alle drei Kanäle gleich an, Rot nur einen (`dR − max(dG,dB) > 30`). Damit 0. (2) `getComputedStyle(pfad).fill` lieferte `""`, weil der Test vorher `zuegeSetzen(false)` gerufen hatte – das leert den Overlay, und an einem abgehängten Element gibt es keinen berechneten Stil. Die Prüfung sah aus wie ein Codefehler und war keiner.
  Pause räumt sie ab und sperrt den Knopf, ein neues Spiel ebenso. Achtzehn Prüfungen in `test_browser`, elf in `test_layout` (drei Chips auf einer Zeile über vier Gerätebreiten bis hinunter zu 320 px).
- **Eine offene Rettung überlebt keinen Vorwärtszug** (`game.rettungOffen=false` in `applyMove` – v1.59). Lutz am 18.09.2026 vermutete zwei Fehler; **beide nachgestellt, beide trafen nicht zu**: „hin und her mit demselben Zug zählt immer wieder" – nein, `rettKeys` hält dagegen (gemessen 1/1/1 über drei Runden); „zurück, ohne bis zur Rettung zu gehen, und wieder vor" – zählte schon vorher nicht.
  **Seine Frage deckte aber einen dritten, echten Fehler auf.** `rettungOffen` blieb nach einem Zurück, das noch nicht gerettet hatte, hängen. Das ist kein Schönheitsfehler: „verloren" heißt `best > lb`, und **die Paritätsschranke `lb` steigt im Lauf der Partie mit** (siehe Warnblitz, v1.35). Erreicht sie `best` – etwa best 2, lb 2 –, gilt die Stellung als nicht mehr verloren, und die hängende Merkzelle zählte **eine Rettung, die es nie gab**. Gemessen am konstruierten Zustand: 1 statt 0.
  **Die Regel ist jetzt eindeutig: eine Rettung entsteht ausschließlich durch Zurücknehmen.** Jeder Vorwärtszug löscht die offene Rettung. Sechs Prüfungen in `test_browser` (konstruierter Fall, hin und her dreimal, offen nach dem Zurück, gelöscht nach dem Vor, Teilrückzug zählt nicht, erst das Zurück bis zur lösbaren Stellung zählt).
  **Zwei richtige Vermutungen zu prüfen kostet weniger, als eine falsche zu übernehmen** – und hier fand die Prüfung etwas, wonach niemand gesucht hatte.
- **Welches Feld gemeint ist, wird gerechnet – nicht getroffen** (`feldAn`, `TAP_MAX` = `P*0.75` – v1.57). Lutz am 17.09.2026: „Ich klicke hier im Lernen Modus die Steine manchmal an und es passiert nichts."
  **Ursache**: Bis v1.56 entschied `e.target.closest('[data-idx]')`, also das getroffene Element – ein transparenter Kreis mit `R_HOLE+8` = 33 Einheiten. Zwischen zwei Feldern lagen damit `100 − 2·33 = 34` Einheiten, in denen ein Tipp **nichts** traf. Gemessen auf dem iPhone in Safari: **21 px Trefferfläche, 11 px tote Zone** – Apple verlangt 44 pt. Schon 9 px diagonaler Fingerversatz ging ins Leere.
  **Jetzt das nächstgelegene Feld**: Berührungspunkt über `getScreenCTM().inverse()` in Brettkoordinaten, dann das Feld mit dem kleinsten Abstand. Die Trefferfläche ist damit die **Voronoi-Zelle** – lückenlos, und sie wächst automatisch mit jeder Brettgröße. Gemessen: **44 px** (14 Pro, bei 43,6 px Rasterabstand – die Zelle ist voll ausgeschöpft), **34 px** in der Lektion (vorher 21), 22 px auf dem iPhone SE bei 21,2 px Raster.
  **`TAP_MAX` = 0,75 Rasterabstände, nicht 0,7**: die Diagonale zwischen vier Feldern liegt bei 70,7 Einheiten; mit 0,7 blieben dort winzige tote Ecken – auf dem iPhone SE fielen dadurch **100 von 825 Rasterpunkten** durch. Nach außen begrenzt die Schwelle weiterhin, sonst zöge ein Tipp weit neben dem Brett noch einen Stein an.
  **Die Wisch-Schwelle bleibt bei festen 18 px.** Ein erster Entwurf machte sie proportional zur Brettgröße – auf dem kleinen Lektionsbrett wäre sie damit auf 14 px **gefallen** und hätte noch mehr Tipps als Wisch gedeutet, also genau das Gegenteil bewirkt. **Wie weit ein Finger beim Tippen verrutscht, hängt am Finger, nicht am Brett.** 18 px liegen auch dort unter einem Feldabstand (32 px), Wischen bleibt möglich. Zurückgenommen, weil ungemessen.
  **Die Prüfgröße ist nicht eine absolute Pixelzahl**, sondern ob das Feld seine Zelle ausschöpft (≥ 85 % des Rasterabstands) – mehr als die halbe Strecke zum Nachbarn **kann** es nicht bekommen. Gemessen wird der kleinste Versatz in acht Richtungen, bei dem noch dasselbe Feld gemeint ist, nicht der Radius irgendeines Kreises. **Auch das Testraster muss relativ sein**: feste 14 px liegen auf einem Brett mit 21 px Rasterabstand schon außerhalb jeder Zelle, und der Test meldete acht solcher Punkte als „tote Zone" – ein Fehler im Test, nicht im Code. Sechs Prüfungen in `test_layout` über drei Gerätehöhen.
- **Das Brett in den Lektionen: drei Fehler, die sich addierten** (v1.56). Lutz am 17.09.2026: „Da ist aber manchmal das Spielfeld extrem klein." Gemessen, bevor etwas geändert wurde – Brettbreite normal gegen Lektion:
  iPhone 14 Pro im Vollbild 366 → 342, **Safari mit Leisten 366 → 228** (Lutz' Fall: 38 % weniger Breite, **61 % weniger Fläche**), iPhone SE 316 → 154, 320 × 568 → 100 (die Untergrenze `MIN`), und dort ragte die **Fußleiste 161 px aus dem Bild**.
  (1) **Der ausführliche Erklärtext stand ganz in der Leiste** – 95 von 170 px. Er geht jetzt über `setStatus(L.intro,'',null,null,L.steps)` in die Statuszeile und damit ins Blatt hinter dem Pfeil, wo in dieser App **jeder** lange Text steht; `#lessonBar .lx` ist auf zwei Zeilen geklemmt (`-webkit-line-clamp`). Der Schlusstext `L.done` ebenso.
  (2) **Die Reserve in `fitStage` war die feste Zahl 230** – geraten, nicht gemessen, während der Sockel seit v1.30 misst. Jetzt `lessonBar.offsetHeight + max(82, statusEl.offsetHeight)`. **Erster Versuch daneben**: mit `statusReserve()` statt der wirklichen Höhe sank das Brett auf dem iPhone SE von 154 auf 100 px – `statusReserve()` misst die **längste denkbare** Meldung, in einer Lektion sind die Meldungen aber kurz und einzeilig.
  (3) **`stageShrink` wächst nur und wurde beim Wechsel ins Lektions-Layout nie geleert.** Es wird sonst allein bei einem `resize` zurückgesetzt (`ensureStatusFits` gibt Platz absichtlich nur nach unten ab, damit das Brett im Spiel nicht springt). Der alte Wert aus dem normalen Spiel fraß weiter Brettfläche – **gemessen 108 px, die ungenutzt blieben**. `startLesson`, `lessonNextPhase` und `quitLesson` setzen ihn jetzt zurück. **Reihenfolge beachten**: erst `setStatus` (das ruft selbst `ensureStatusFits` und setzt `stageShrink` neu), dann `stageShrink=0; fitStage(); ensureStatusFits()` – umgekehrt bleibt der Wert stehen und die Hälfte der Wirkung aus (einmal erlebt).
  **Ergebnis gemessen**: 14 Pro 342 → **366** (volle Breite), Safari 228 → **270**, SE 154 → **196**, 320 × 568 ragt statt 161 px nur noch 15 px heraus.
  **Die Purges waren nie unsichtbar**: Paket gepunktet, Hilfsstein grün – das gab es seit jeher, Lutz hat es bei 228 px Brett nur nicht erkannt. Eine Prüfung wacht jetzt darüber, dass die Ringe da sind; **ohne sie könnte eine spätere Platzsparmaßnahme genau das wegnehmen, worum es in der Lektion geht.** Fünfundzwanzig Prüfungen in `test_layout` über drei Gerätehöhen (Brett ≥ 55 % der normalen Breite, Fußleiste im Bild, Leiste verdeckt sie nicht, Ringe vorhanden, Text höchstens zwei Zeilen, Einleitung im Blatt).
- **Die Sinnesgrenze ist die LETZTE vor dem Limit, nicht die erste** (`kurzfassung`, gieriges `(.*)` statt `(.*?)` – v1.55). Lutz' Screenshot 17.09.2026: „Zurück: Violett zurück, Gold…" – der nicht-gierige Ausdruck griff den Doppelpunkt nach „Zurück" (6 Zeichen, unter `KURZ_MIN`) und fiel in Stufe 3 mit Auslassungspunkten, obwohl das Komma bei 22 Zeichen passte. Der Test „Spultexte passen in eine Zeile" war grün, weil er nur maß, ob der **gekürzte** Text passt – nicht, ob richtig gekürzt wurde. Stufe 3 streift jetzt zusätzlich Satzzeichen vor den Punkten („zurück,…" → „zurück…"). Neun Prüfungen; die Spulmeldungen tragen seit v1.55 eine **ausdrückliche** Kurzfassung (`lastMove.kurz`: „Vor: Bernstein über Bernstein", 29 Zeichen), weil „Vor: Bernstein springt über Bernstein." mit 38 Zeichen keine Sinnesgrenze hat.
  **Das Blatt zeigt den Satz einmal** (`openDetail`): Ist die Kurzfassung nur ein abgeschnittener Anfang des Textes, stand oben der Stummel und darunter der ganze Satz – Lutz: „macht keinen Sinn". Dann trägt der Titel nur das Stichwort vor dem Doppelpunkt („Zurück", „Vor"). Eigenständige Kurzfassungen („1 Stein bleibt erreichbar") bleiben Titel; Gegenprobe im Test.
- **Kurzfassungen werden in drei Stufen gekürzt** (`kurzfassung`, `KURZ_MAX` 32, `KURZ_MIN` 10, `KURZ_BINDE`): (1) erster Satz, wenn er passt; (2) sonst an der letzten **Sinnesgrenze** davor – Gedankenstrich, Komma, Semikolon, Doppelpunkt – und dort **ohne** Auslassungspunkte, weil der Teil für sich einen Sinn ergibt; (3) erst wenn es keine gibt, am Wortende mit Auslassungspunkten, und dann **ohne Bindewort am Schluss**. Bis v1.30 gab es nur Stufe 3, und die ließ den Leser im Nebensatz hängen: aus „Tippe einen Stein an – bei mehreren Zielen wische in die Richtung.“ wurde „Tippe einen Stein an – bei…“ (Lutz’ Screenshot 21:03 Uhr). Jetzt „Tippe einen Stein an“. Damit brauchen die meisten Meldungen **keine handgeschriebene Kurzfassung mehr** – die bleiben für die Fälle, in denen ein anderer Wortlaut besser ist als ein Ausschnitt. Fünf Prüfungen in `test_browser` über fünfzehn echte Meldungen, dazu eine im Gerät (der sichtbare Text darf nicht abgeschnitten werden). **Achtung beim Prüfen**: „Tippe einen Stein an“ endet auf „an“ und ist trotzdem ein ganzer Satz – als Bruch zählt nur eine Kürzung **mit** Auslassungspunkten, die auf einem Bindewort endet.
- **Das Markierungszeichen erklärt sich selbst** (`istGestrandet`, `MARKE_TEXT`, `MARKE_KURZ`): Lutz fragte am 13.09.2026, was das ⊗ an einem Stein bedeutet – es stand nirgends außer in der Menüzeile. Jetzt zweifach: (1) **Ein Tipp auf den markierten Stein** sagt „Dieses Feld bleibt in jeder bestmöglichen Fortsetzung besetzt – hier wird ein Stein übrig bleiben“, jederzeit wiederholbar; löst der Tipp einen Zug aus, bleibt die Erklärung weg – wer den Stein bewegt, braucht sie nicht. (2) **Beim allerersten Auftauchen** eine schwebende Meldung (`toast`, 4,2 s, einmal überhaupt, gemerkt in `markeErklaert`) – einmal je Partie wäre bei zwanzig Partien am Abend zwanzigmal zu viel. Bewusst **`toast` statt der Statuszeile**: über `hinweis()` hätte sie 2,2 s Vorrang und würde die gerade fertige Bewertung verdrängen – genau das machte beim ersten Versuch eine ältere Prüfung rot. Formulierung: **Feld**, nicht Stein – `optFinal` sagt, dass das *Feld* in jedem Endbild besetzt ist, nicht dass derselbe Stein dort bleibt. Sieben Prüfungen in `test_browser` (an Lutz’ Stellung vom 13:57 Uhr unabhängig nachgerechnet), zwei in `test_layout`.
- **Alarmton zur Wahl** (`ALARM_TOENE`, `settings.alarmTon`, `alarmWahl()`, Standard **`gong`**): Fünf Entwürfe plus **Aus**, im Menü als Chips unter dem Warnblitz; ein Tipp wählt **und spielt sofort zur Probe ab** – ohne Hören ist der Name nur ein Wort. Ein gespeicherter Name, den es nicht mehr gibt, fällt über `alarmWahl()` auf den Gong zurück – still ist genau das, was ein Alarm nicht sein darf. Alle beginnen erst nach 0,06 s, damit der Sprungton des Zuges den Anfang nicht zudeckt. Die Sperre für den kurzen Fehlton hängt an `alarmDauer()`, nicht an einer festen Zahl.
- **Der Gong ist eine Klangschale** (Schlüssel bleibt `gong`, Menüname „Klangschale“ – der Schlüssel ist intern, sonst wäre Lutz’ gespeicherte Einstellung verloren). Von Lutz am 13.09.2026 aus vier Entwürfen gewählt. Zwei Schichten: **der Anfang** – fünf kurze Stimmen (0,12–0,28 s), darunter 924 und 1232 Hz, die es später nicht mehr gibt; das ist das metallische Aufblühen einer echten Schale. Bewusst **tonal statt als Rauschimpuls**: ein Rauschanschlag klackt, und Lutz wollte „nicht klacken, sondern mehr Gong“. **Der Klang danach** – Grundton 220 Hz doppelt besetzt mit 1,5 Hz Abstand, die beiden überlagern sich zu einem Pulsieren alle 0,67 s; ohne diese Schwebung steht ein synthetischer Gong still. Teiltonverhältnisse 1 : 1,5 : 2,1 : 2,8, bewusst **nicht** ganzzahlig – sonst klänge es nach Orgelpfeife. **220 Hz und nicht tiefer**, weil ein Telefonlautsprecher unter 150 Hz kaum etwas hergibt: diese Lage hat die dreifache Energie oberhalb davon wie der tiefe Gong, den sie ablöst. Hauptstimmen auf 64 % ihres Pegels, damit die Summe bei 0,93 bleibt.
- **Lautstärke = Summe der gleichzeitig klingenden Stimmen**, gewichtet mit dem Effektivwert der Wellenform (Rechteck 1, Sinus 1⁄√2, Sägezahn und Dreieck 1⁄√3) – **nicht** die lauteste Einzelstimme. Diese Prüfung maß zweimal falsch: in v1.24 verglich sie rohe Zahlenwerte, danach Spitzen von Einzelstimmen, und wurde prompt rot, als der Gong elfstimmig wurde. Mit der richtigen Größe fiel auf, dass Martinshorn, Absturz und Buzzer nur halb so laut waren wie der Sägezahn – behoben **durch mehr Stimmen, nicht durch höheren Pegel** (ein Sägezahn bei 0,36 kreischt, drei Stimmen bei 0,20/0,10/0,07 tragen genauso weit und klingen voll). Schwelle im Test: Sprungton × 1,2 – der Alarm kommt direkt danach und muss sich gegen ihn durchsetzen. Dazu eine Prüfung, dass die Summe nie 1,0 erreicht (darüber übersteuert es hörbar) und dass die Alarme außer dem Gong dicht beieinander liegen. **Sechzehn Prüfungen in `test_browser`.**
- **Siegfanfare** (`Sound.win()`): Auftakt-G mit Oktave, aufsteigender C-Dur-Dreiklang, dann ein fünfstimmiger Schlussakkord (c′ e′ g′ c″ über C als Fundament, 0,95 s) plus ein leiser Glanzton g‴. Mehrstimmig, weil ein einzelner Oszillator dünn klingt; läuft 1,49 s. Gerufen wird sie bei `left===1` und bei „Lektion geschafft“. Drei Prüfungen in `test_browser` (mehrstimmig, Schlussakkord, nicht leiser als der Sprungton). **Töne kann kein Test hören** – Lautstärke und Klang beurteilt Lutz am Gerät.
- **Audio-Kontext auf iOS** (`Sound.ensure`, `Sound.wartend`): iOS legt den Kontext schlafen (`suspended`, iOS-eigen auch `interrupted`) – nach einem Anruf, beim Wegschalten der App, teils nach einer Pause. Zwei Fallen: `resume()` wirkt erst später, währenddessen steht `currentTime` still, ein darauf geplanter Ton liegt nach dem Aufwachen in der Vergangenheit und wird verschluckt; und iOS erlaubt `resume()` nur aus einer **Nutzergeste**, während Fanfare und Alarm aus der Bewertung kommen, also aus einem Timer. Deshalb: jedes `pointerdown` auf `document` (capture, passiv) weckt den Kontext, `visibilitychange` ebenso, und `tone()` stellt Töne in `wartend` zurück, bis der Kontext `running` ist – nachgeholt wird nur, was jünger als 400 ms ist (ein Sprungton von vor zehn Sekunden wäre Unfug). Drei Prüfungen in `test_browser`.
- **Fehlerklassen** (`strategyAdvice`): Reihenfolge – Falsche Richtung – Stein gestrandet – Struktur. Seit v1.22 fällt auch **dasselbe Zielfeld von der falschen Seite gefüllt** unter Falsche Richtung (`better.to===played.to`, Begründung mit beiden Richtungen). Vorher landete dieser Fall bei „Struktur“ und war nicht zu verstehen – belegt an Lutz’ Stellung vom 13.09. Die Klasse nennt jetzt **auch der Trainer**, nicht nur die Strategie-Hinweise: `strategyAdvice` ist aus der vorliegenden Bewertung praktisch gratis, und wer die Strategie-Hinweise aus hat, stand sonst vor „Der letzte Zug hat das gekostet“ ohne jede Erklärung. Damit unterscheiden sich beide Schalter fast nur noch im Tonfall – ein Zusammenlegen steht aus, Entscheidung von Lutz.
- **Hinweis auf eine neuere Version** (`pruefeUpdate`, `zeigeUpdate`, `versionNeuer`): 2,6 s nach dem Start und bei jeder Rückkehr aus dem Hintergrund (höchstens alle 25 Minuten) holt die App den **Anfang** ihrer eigenen Datei vom Server – Bereichsanfrage `bytes=0-49151`, weil `APP_VERSION` bei Byte ≈ 24.600 von 132.000 steht; statt 129 kB reichen 48. Ignoriert ein Server die Bereichsanfrage, kommt die ganze Datei und es funktioniert trotzdem; schlägt das Laden fehl (offline, `data:`-Kontext), passiert schlicht nichts. Ist die Nummer dort höher, erscheint ein **schwebender** Streifen (`#update`, `position:fixed`) – im Fluss würde er über `fitStage` Brettfläche kosten, ein Test wacht darüber. Tippen lädt mit `?v=<neu>` neu (der angehängte Wert umgeht den Zwischenspeicher des iPhones), das × legt den Streifen weg. `versionNeuer` vergleicht **stellenweise als Zahl** – als Text wäre „1.9“ größer als „1.22“, und genau dort steht die Zählung. Zwölf Prüfungen in `test_browser`, zwei in `test_layout`.
- **Icon** (`2-icon.png` 180 px, `2-icon-1024.png` 1024 px): Foto eines Nussbaumbretts mit bunten Murmeln, von Lutz beigesteuert. Entscheidend war der Vergleich bei 58 px – der Größe auf dem Home-Bildschirm: Große, satte Murmeln mit Schatten ergeben dort noch ein erkennbares Kreuz, das früher gerenderte Brett zerfiel in Pixelrauschen. Nicht die Zahl der Elemente entscheidet, sondern ob sie zusammen eine Form ergeben. 16 px des Originals ringsum beschnitten (helle Ecken).
- **Standardthema** bleibt `glas` („Samt & Glas“, blaues Brett). Die Nussbaum-Optik aus dem Startbild ist als Thema „Nussbaum & Glas“ wählbar und liefert zugleich das Motiv für das Home-Bildschirm-Icon (`2-icon.png`), aber nicht das Spielbrett.
- **Versionszählung (Vorgabe von Lutz).** Die zweite Stelle zählt die **Auslieferungen**, die etwas Sichtbares verändert haben – `index.html` oder die Icons: v1.1 war der hochgeladene Stand, danach 16 weitere → v1.17. Reine Doku- oder Test-Commits zählen nicht mit (`git log --oneline <stand>..HEAD -- index.html | wc -l`). Die erste Stelle bleibt für neue Substanz reserviert – Lernsystem, Offline-Betrieb, ein weiteres Brett. `APP_VERSION` ist die einzige Quelle; sie steht im Startbild (`#splashVersion`) und im Menü, beide werden im Test dagegen geprüft.
- **v1.61** (20.09.2026): Die weißen Dreiecke bleiben jetzt stehen, bis du den Knopf wieder drückst – kein Ausblenden nach drei Sekunden mehr. Die Einstellung überlebt auch einen Neustart.
- **v1.60** (20.09.2026): Ein Knopf zeigt alle Züge, die gerade möglich sind – weiße Richtungsdreiecke, drei Sekunden lang. Schon probierte Züge bleiben rot.
- **v1.59** (18.09.2026): Eine angefangene Rettung wird verworfen, sobald du wieder vorwärts gehst – sonst zählte sie später einmal zu viel, wenn die Paritätsschranke mitstieg.
- **v1.58** (18.09.2026): Eine Rettung wird jetzt gezählt, egal über wie viele Schritte du zurückgehst – und auch dann, wenn du zügig weitergespielt hast, bevor die Bewertung fertig war. Vorher blieb der Zähler in genau diesem Fall auf null.
- **v1.57** (17.09.2026): Ein Tipp trifft jetzt immer den Stein, den man meint – gerechnet wird das nächstgelegene Feld statt das getroffene Element. Die Trefferfläche wächst von 21 auf 34 px in den Lektionen, tote Zonen dazwischen gibt es keine mehr.
- **v1.56** (17.09.2026): In den Lektionen ist das Brett wieder groß – der lange Erklärtext steht jetzt im Blatt hinter dem Pfeil statt auf dem Spielfeld. In Safari wächst es von 228 auf 270 px.
- **v1.55** (17.09.2026): Spulmeldungen werden an der richtigen Stelle gekürzt („Zurück: Violett zurück" statt „…Gold…"), und das Blatt hinter dem Pfeil zeigt den Satz nur noch einmal.
- **v1.54** (17.09.2026): Alle Hilfen in zwei Schaltern – **Computer** (Ampel, Blitz, Erklärung, Markierung, Dreiecke) und **Tipps**. Computer aus heißt: wie am echten Brett, nur Vor und Zurück. Trainer und Strategie-Hinweise sind darin verschmolzen.
- **v1.53** (16.09.2026): Der Knopf im Erklär-Blatt nimmt den Zug jetzt nur noch zurück, statt gleich den besseren zu verraten. Tipps kostet nur noch der Tipp-Knopf – so weiß man, wann man wirklich einen verbraucht.
- **v1.52** (16.09.2026): Ein probierter Zug ist jetzt ein rotes Richtungsdreieck – breit, wo man ansetzte, spitz, wo man landete. Man sieht die Richtung, ohne dass es ein Pfeil wird.
- **v1.51** (16.09.2026): Der Pfeil rechts erklärt jetzt, warum ein Zug schlecht war, und sagt, wo zu suchen ist – statt den richtigen Zug zu verraten. Die Fehlersuche kostet dadurch keinen Tipp mehr.
- **v1.50** (16.09.2026): Der rote Strich eines probierten Zuges läuft jetzt bis in die Punkte an seinen Enden – vorher klaffte dazwischen eine Lücke. Die Punkte sind etwas größer.
- **v1.49** (16.09.2026): Die probierten Züge haben kleine Punkte an beiden Enden statt eines dicken Rings am Ziel. Und der bessere Zug wird nicht mehr ungefragt verraten – wer ihn sehen will, zahlt einen Tipp dafür.
- **v1.48** (15.09.2026): Die Striche der probierten Züge sind jetzt kräftig statt zaghaft – doppelt so stark und 2,3-mal so viel Fläche.
- **v1.47** (15.09.2026): Zurückgenommene Züge bleiben als dünner roter Strich stehen – so sieht man, was man in dieser Stellung schon probiert hat. Die Ampel steht ab Werk auf Stufe 6.
- **v1.46** (15.09.2026): Bei Rot flackert wieder der ganze Bildschirm wie beim Warnblitz – Grün und Gelb bleiben der schmale Schein unten.
- **v1.45** (15.09.2026): Beim Loslassen des Ampelreglers läuft jetzt die volle Probe – die Ampel blitzt, der Schein schwillt an und ab. Und er liegt beim Einstellen vor dem Blatt statt darunter, wo ihn nie jemand gesehen hat.
- **v1.44** (15.09.2026): Der Ampelregler hat jetzt elf Positionen statt fünf, und der Schein über dem Bild wächst abgestuft mit, statt nur ganz oben anzugehen.
- **v1.43** (15.09.2026): Langer Druck auf die Ampel öffnet einen Regler – fünf Stufen von „wie bisher“ bis zu einem Schein über dem ganzen Bild, mit Vorschau beim Ziehen.
- **v1.42** (14.09.2026): Der Glanz gehört dem Stein, der liegen bleibt – wer sofort wegspringt, glänzt nur noch an seinem Ziel.
- **v1.41** (14.09.2026): Der Glanz kommt jetzt bei jeder Berührung eines Steins – vorher blieb er bei Auto-Sprung und bei Steinen ohne Zug aus. Dazu länger und kräftiger.
- **v1.40** (14.09.2026): Das Funkeln war unsichtbar, wenn „Bewegung reduzieren“ eingeschaltet ist – der Streifen stand neben der Murmel. Jetzt ohne Ausschnitt, und die Prüfung zählt Bildpunkte statt Attribute.
- **v1.39** (14.09.2026): Die Murmeln funkeln – ein Streiflicht wandert über den Stein, den man antippt, und über den, der gerade gesprungen ist.
- **v1.38** (14.09.2026): Die Pause ist jetzt ein sichtbarer Chip neben den Strategie-Hinweisen – das Zeichen an der Uhr war zu klein zum Finden.
- **v1.37** (14.09.2026): Der Weiter-Knopf in der Pause ist jetzt ein großes Quadrat genau in der Mitte des Bretts statt einer Zeile quer darüber.
- **v1.36** (14.09.2026): Rettungen und Tipps stehen im Ergebnis als eigene Zahlen neben der Steinzahl statt im Fließtext; die Zeit lässt sich anhalten.
- **v1.35** (13.09.2026): Der Warnblitz kommt jetzt zuverlässig – die Bedingung verglich gegen die falsche Schranke, und ohne fertige Vorbewertung kam er gar nicht. Trainer und Strategie-Hinweise werden einmalig eingeschaltet.
- **v1.34** (13.09.2026): Tipps und Rettung als Block aus vier Zellen rechts in der Zählerzeile – Wort und Zahl nebeneinander, zwei Zeilen untereinander.
- **v1.33** (13.09.2026): Tipps und Rettungen stehen jetzt als vierte und fünfte Spalte in der Zählerzeile; „Rettung“ statt „Rücknahme“.
- **v1.32** (13.09.2026): Zwei Zähler für die Partie – Rücknahmen nach einem Verlustzug und verbrauchte Tipps, klein neben dem Strategie-Knopf und im Ergebnis.
- **v1.31** (13.09.2026): Meldungen brechen nicht mehr mitten im Nebensatz ab – gekürzt wird an der nächsten Sinnesgrenze.
- **v1.30** (13.09.2026): Der Sockel misst jetzt, wie viel Schutzrand er wirklich braucht – in Safari lag unter dem Blatt ohnehin ein freier Streifen, der Rand war doppelt.
- **v1.29** (13.09.2026): Der Sockel mit dem Fertig-Knopf von 99 auf 83 px – mehr geht nicht, 34 davon sind der Schutzrand des Geräts.
- **v1.28** (13.09.2026): Fehler aus v1.27 behoben – das Menü war auf 134 px zusammengeschrumpft. Ursache war `flex-basis:0` statt `auto`.
- **v1.27** (13.09.2026): Der Fertig-Knopf steht in Menü und Blatt fest unten – kein Scrollen mehr, nur um zu schließen.
- **v1.26** (13.09.2026): Der Gong ist jetzt eine Klangschale mit kräftigem, tonalem Anfang – ohne Klacken. Martinshorn, Absturz und Buzzer angehoben, nachdem die Lautstärkeprüfung auf die richtige Größe umgestellt wurde.
- **v1.25** (13.09.2026): Das Zeichen am gestrandeten Stein erklärt sich jetzt selbst – einmal von allein beim ersten Auftauchen, danach auf Tipp.
- **v1.24** (13.09.2026): Der Alarmton ist wählbar – fünf Entwürfe plus Aus, Probe beim Antippen. Ab Werk der Gong.
- **v1.23** (13.09.2026): Die App merkt selbst, wenn auf dem Server eine neuere Version liegt, und bietet sie zum Laden an – Antwort auf den ausgelassenen Pages-Build, bei dem nur die Nummer im Startbild verriet, dass Änderungen fehlten.
- **v1.22** (13.09.2026): Ton verstummte auf dem iPhone, sobald der Audio-Kontext eingeschlafen war – jetzt weckt ihn jede Berührung, Töne warten statt verloren zu gehen. Neue Fehlerklasse für ein von der falschen Seite gefülltes Loch; der Trainer nennt die Fehlerklasse jetzt selbst.
- **v1.21** (13.09.2026): Siegfanfare mehrstimmig statt drei Einzeltöne, Alarm mit drei Tönen und Fundament. Die Fehlton-Unterdrückung nach dem Alarm (1200 ms) deckt den längeren Alarm (0,98 s) weiter ab.
- **v1.2.1** (12.09.2026): Spul-Markierung verschwindet beim Berühren des Bretts, eigener Alarmton beim Verlust der Lösung, neues Standardthema „Nussbaum & Glas“.
- **v1.2** (12.09.2026): Zeile unter dem Brett neu – Ampel mit drei Lichtern, feste Steinzahl, Kurzfassungen, Erklärung im Blatt; Meldungen verschwinden nicht mehr hinter der Fußleiste (Brett nutzt die volle Breite); Lauflicht und Band beim Rechnen; Warnblitz; Texte ohne Störungssprache. Dazu Werkzeug: Tests melden Fehler jetzt über den Exit-Code, neue Layout-Suite, Startskript für Cloud-Sitzungen.
- Weitere Dateien durchnummeriert (`1-sw.js`, `2-icon.png`).
- Nie „Budget" in Nutzertexten. Statuszeile: links eine **Ampel mit drei Lichtern** (Gehäuse 30 × 66 px, `box-sizing:content-box` und `align-items:center` – mit `border-box` saßen die Lichter 3 px zu weit rechts; Test prüft den Versatz; rot = nicht mehr erreichbar, gelb = nicht bewertbar, grün = 1 Stein erreichbar; ohne Bewertung alle gedimmt; beim Rechnen läuft das Licht **von oben nach unten durch**, rot → gelb → grün → rot, lückenlos (0,96 s je Zyklus, nie alles dunkel – Test tastet einen Zyklus ab); ein Pendeln mit Nachleuchten war auf dem iPhone schlechter und ist verworfen. Sobald das Ergebnis steht, **blitzt** das zutreffende Licht einmal auf (`#status.blitz`, 0,5 s) und kommt dann zur Ruhe – `zeigeRechnet()` hängt daran, dass gerechnet wird, **nicht** an der Trainer-Einstellung), in der Mitte zwei Zeilen – oben fest die Steinzahl (`ankerText()`, wird von `render()` nachgezogen), darunter die Meldung in Kurzfassung –, rechts der Knopf zum Blatt (34 px, Winkel nach oben als eigenes SVG – das Zeichen ℹ wird von iOS als blaues Emoji gerendert und ist deshalb unbrauchbar). Ampelpunkt und Knopf stehen auf Höhe der **unteren** Zeile, nicht mittig über beiden. Eine Meldung, die nur die Steinzahl wiederholt, lässt die untere Zeile leer, damit dort nichts flackert. Die untere Zeile ist **hart einzeilig** (`white-space:nowrap` mit Auslassungspunkten) – sonst wandert die Steinzahl beim Wechsel der Meldungen nach oben, und genau diese Unruhe soll die Zeile nicht haben. Schriftgröße 15 px (Steinzahl 16 px) – mehr passt nicht, die Textsäule hat zwischen Ampel und Knopf nur rund 244 px. Kein Wort *Fehler* in Kurzfassungen – das liest sich wie eine Störungsmeldung des Programms; `KURZ_KLASSE` sagt stattdessen, was im Spiel passiert ist (Zu früh – erst etwas anderes / Andere Richtung wäre richtig / Ein Stein strandet / Lücke an wichtiger Stelle). Kurzfassungen höchstens 32 Zeichen; für die häufigen Meldungen als letztes Argument von `setStatus` explizit gesetzt („1 Stein bleibt erreichbar", „Bestenfalls N Steine", „Fehler: <Klasse>", „Noch nicht bewertbar"). Unter der Zeile läuft beim Rechnen zusätzlich ein Band durch (`#rechenband`) – Lutz will es als zweites, klares Zeichen; es läuft **im selben Takt** wie die Ampel (0,96 s), sonst driften beide gegeneinander und wirken, als wechselten sie sich ab (Test prüft den Gleichtakt). Der Text bleibt beim Rechnen stehen. Begründung, Zusatzzeile und Aktionen stehen im Blatt `#detail`, das ein Tippen auf die Zeile öffnet; `statusFullText()` liefert den vollen Wortlaut. So bleibt das Brett so groß, wie die Bildschirmbreite erlaubt (iPhone 14 Pro: 369 px).
- Nichts raten, was Lutz in wenigen Handgriffen prüfen kann; Diagnosen in die Ausgabe schreiben; Tests dürfen nicht dieselbe Annahme treffen wie der Code (Literaturwerte, unabhängige BFS).
- Keine Zustimmungsfloskeln; Widerspruch mit Grund.

## Wann ein neuer Chat fällig ist

**Regel von Lutz (16.09.2026):** „Wir sollten irgendwie einbauen, wann du mir einen Ratschlag gibst, einen neuen Chat aufzumachen."

**Der Auslöser ist die Zusammenfassung der Sitzung, nicht ein Gefühl.** Wird der Chat zu lang, faltet Claude Code ihn selbsttätig zusammen („This session is being continued…"). Das ist ein objektives Ereignis im Protokoll, und `tools/kosten.py` zählt es mit: Bei jedem Lauf schreibt es auf **stderr** eine Zeile `HINWEIS AN CLAUDE …` mit der Zahl der Faltungen. Die Kostenzeile auf stdout bleibt davon unberührt – sie ist weiterhin die eine Zeile, die unter die Antwort gehört. Der Hinweis wird **nie** an die Antwort angehängt.

**Gemeldet wird einmal je neuer Faltung**, nicht bei jeder Antwort – sonst ist es Gemecker. Steigt die Zahl (zweite, dritte Faltung), ist das ein neuer Anlass und wird erneut genannt, dann dringlicher.

**Die Begründung ist der mitgeschleppte Kontext, nicht die Faltung selbst.** Am 16.09.2026 nachgerechnet, nachdem ich zuerst falsch zugeordnet hatte: Eine Faltung kostet rund **1 $** – die 5,77 $, die ich ihr zugeschrieben hatte, waren die Arbeit *danach*. Teuer ist etwas anderes: Bei jedem einzelnen Modellaufruf wurden **230.000 Token** Kontext mitgelesen. An diesem Tag summierte sich das auf **25,8 Mio. Token = 12,91 $ von 24,45 $** – **über die Hälfte der Tageskosten war reines Mitschleppen.** Ein frischer Chat startet bei rund 20.000 Token, also mit dem Elffachen an Luft.

**Was der Wechsel NICHT bringt: Tempo.** Die Wartezeit steckt in `npm test` und im Pages-Build, nicht in der Chatlänge. Das ehrlich dazusagen, sonst ist die Erwartung falsch.

**Was mitgenommen werden muss:** nichts aus dem Chat – `CLAUDE.md` ist das Gedächtnis und liegt im Repo. Offene Entscheidungen gehören deshalb **vor** dem Wechsel unter „Offene Ideen", sonst gehen sie verloren.

**Für alle Sitzungen, nicht nur dieses Projekt:** Diese Datei gilt nur für `solitaire`, und `~/.claude/CLAUDE.md` überlebt einen frischen Cloud-Container nicht verlässlich (das Verzeichnis wird beim Start neu aufgesetzt). Sitzungsübergreifend wirken nur Lutz’ **persönliche Präferenzen in den Claude-Einstellungen**; der Textbaustein dafür steht in `NEUER-CHAT.md`.

## Kostenanzeige

Unter **jeder** Antwort steht die Ausgabe von `tools/kosten.py`, wörtlich als letzte Zeile – nichts dahinter, nicht umformatiert, nicht geschätzt. Ablauf: `python3 tools/kosten.py` ausführen, Ausgabe anhängen. Ohne Ausnahme, auch bei kurzen Antworten. Läuft das Skript nicht (kein Sitzungsprotokoll, andere Umgebung), das offen sagen statt eine Zahl zu erfinden.

Eine einzige Zeile in `<sub>…</sub>`: Uhrzeit · **Arbeit** · Frage · heute · gesamt, deutsches Zahlenformat, Dollarzeichen nur am Ende.

**Die Arbeitszeit steht seit dem 20.09.2026 mit drin** (Wunsch von Lutz). Gemeint ist die **verstrichene** Zeit von seiner Frage bis jetzt, nicht Rechenzeit – genau die Größe, die ihn interessiert: `npm test` braucht rund 5 Minuten, der Pages-Build eine weitere, und er legt das Gerät solange weg. Format: unter 90 s in Sekunden, darunter… darüber in Minuten, ab einer Stunde `1 h 05`. **Zwei Randfälle, beide geprüft**: 3599 s hätten gerundet „60 min" ergeben – unterhalb einer Stunde wird auf 59 gedeckelt, und die Grenze bleibt bei 3600 s, sonst stünde dort „0 h 59". Und wird das Skript **später noch einmal von Hand** gerufen, läge zwischen Frage und `jetzt` auch Lutz' eigene Lesezeit; deshalb endet die Messung beim letzten Protokolleintrag, sobald der über zehn Minuten alt ist (mit einem gefälschten Protokoll nachgestellt: Frage vor 120 min, letzter Eintrag vor 110 min → „Arbeit 10 min" statt „2 h 00"). **Zwei eng stehende Zeilen sind in der Claude-App nicht möglich** – `<br>`, zwei Leerzeichen und Backslash am Zeilenende werden verworfen, eine Leerzeile reißt 133 statt 82 px auf, ein Codeblock baut einen Kasten mit Kopierknopf drumherum. Alles durchprobiert, deshalb eine Zeile.

Diese Fassung (`KOSTENZEILE.md`, 15.09.2026) **ersetzt** die Tabelle (`KOSTENTABELLE.md`); es gilt immer die zuletzt hochgeladene. `tools/kostentabelle.py` bleibt als Werkzeug liegen, wird aber nicht mehr gerufen – wer eine Spalte je Dienst will, lädt die Tabellen-Fassung erneut hoch.

**Nicht jeder `user`-Eintrag im Protokoll ist eine Frage von Lutz** (gefunden von Lutz, 16.09.2026). Hintergrund-Benachrichtigungen (`<task-notification>`), System-Erinnerungen und die Meldung beim Zusammenfassen der Sitzung („This session is being continued…") stehen dort genauso als `user` – und setzten den Zähler „Frage" zurück. Gemessen: um 08:49 Uhr stand **„Frage 0,79 · heute 12,14"**, obwohl beide Beträge zu **derselben** Arbeit gehörten; die Benachrichtigung dazwischen hatte den Schnitt gesetzt. Lutz fiel es auf, weil bei der ersten Frage eines Tages beide Zahlen gleich sein müssen. `echt()` verlangt deshalb einen Textblock, der **nicht** mit `<` oder `[` beginnt. Geprüft an fünf Einträgen des 16.09.: drei echte Fragen, zwei Gerüst-Einträge korrekt aussortiert.

Zwei weitere Fallstricke, die das Skript abfängt: Cloud-Sitzungen laufen mit dem **Stunden-Cache** (doppelter Schreibpreis, `--ttl5` rechnet mit fünf Minuten), und Modellnamen im Protokoll tragen ein Datum – deshalb Präfix-Vergleich, sonst greift der Rückfallpreis (bei Haiku das Fünffache). Die Token der gerade entstehenden Antwort fehlen noch und tauchen erst beim nächsten Mal auf. Fremde Dienste (Bildgenerierung, Hosting) rechnet diese Fassung **nicht** mit – solche Beträge gehören zusätzlich in den Text.

## Tests (headless Chromium via Puppeteer, einmal `npm install`)
- `npm test` – alle sieben Suiten **nebenläufig** (`tools/tests_parallel.js`), höchstens **drei gleichzeitig**; jede meldet Fehler über den Exit-Code. Einzeln z. B. `npm run test:browser`, nacheinander als Rückfall `npm run test:seq`.
- **Gemessen am 16.09.2026: 474 s nacheinander gegen 228 s nebenläufig** – 52 % weniger Wartezeit bei 477 s Rechenzeit. Wunsch von Lutz: „Wenn das das gleiche Aufwand ist, sollten wir das parallel machen um die Zeit zu verkürzen."
  **Drei gleichzeitig, nicht sieben** – und das ist gerechnet, nicht geschätzt: `test_browser` braucht allein **226 s**, die sechs anderen zusammen **248 s**. Auf zwei freien Spuren neben `test_browser` sind das 124 s, also weit unter dessen 226 s. `test_browser` ist damit der Deckel; mehr Nebenläufigkeit verkürzt **nichts** und belastet die Maschine nur zusätzlich. Wer noch schneller will, muss `test_browser` selbst aufteilen.
  **Warum die Grenze überhaupt zählt: mehrere Suiten MESSEN Zeit** – `test_motion` die Animationsdauern (Zug 230 ms, Spulen 400 + 650 ms), `test_browser` und `test_layout` den Gleichtakt von Ampel und Band (0,96 s). Läuft die Maschine am Anschlag, werden diese Messungen langsamer und die Prüfungen wackeln. **Eine parallele Testinfrastruktur, die die Messungen verfälscht, ist schlimmer als eine langsame.** Deshalb beide Läufe verglichen: Zug 239 gegen 239 ms, Spulen 1076 gegen 1073 ms, Gleichtakt 0,96/0,96 in beiden, Glanz 2496 gegen 2498 Bildpunkte, `test_browser` 227 gegen 226 s. Kein messbarer Einfluss – aber bei jeder Erhöhung der Grenze **neu prüfen**.
  Die Ausgabe wird **je Suite gesammelt** und am Stück gedruckt, nicht verschränkt – gerade die Diagnosen in der Ausgabe sind ihr Zweck. Die Nebenläufigkeit lässt sich über `TEST_PARALLEL=<n>` ändern. Keine Namenskollision bei den Screenshots geprüft: `test_browser` schreibt `shot_splash/game/hint/result/spool/sheet/board_*/theme_*`, `test_lessons` nur `shot_menu_lessons` und `shot_lesson5`.
- `node tests/test_browser.js` – 342 Prüfungen (Spiel, Tipp, Trainer, Markierung, Strategie, Fehlersuche, Spulen, Textbreiten, Alarmtöne, Update-Hinweis).
- `node tests/test_lessons.js`, `node tests/test_worker.js` (Worker-Ausfallszenarien), `node tests/test_full.js` (everEmpty gegen BFS), `node tests/test_table.js` (Exaktheit gegen Stellungszählung), `node tests/test_motion.js` (Animationsdauern: Zug 230 ms, Spulen 400 ms Vorlauf + 650 ms), `node tests/test_layout.js` (nichts verschwindet hinter der Fußleiste, über sechs Geräte- und Schriftgrößen; Brett springt nicht; Ampel ≥ 20 px und ihre Lichter mittig, Knopf ≥ 30 px, keine Zeichenreste im sichtbaren Text, Steinzahl steht fest und wandert beim Wechsel der Meldungen nicht, untere Zeile bleibt einzeilig, Ampel und Band laufen beim Rechnen im selben Takt und stehen danach still, dabei nie ganz dunkel; Ergebnislicht blitzt genau einmal).
- Werkzeuge: `tools/gen_book2.js` (Eröffnungsbuch neu rechnen), `tools/purge_find.js`/`purge_find2.js`/`purge_plan2.js` (Purge-Muster und Partie-Plan), `tools/count_pos2.js` (Stellungen zählen), `tools/exp_parity.js` (Paritätsschranke prüfen). Ergebnisse liegen als `tools/patterns.json` (Purge-Muster) und `tools/plan.json` (Partie als Purge-Folge) daneben.

## Offene Ideen
- Service Worker `1-sw.js` für Offline-Betrieb; Splash-Bild aus dem Walnuss-Icon.
- Purge-Sprache in den Strategie-Hinweisen („Hilfsstein für den Sechser verbraucht") – erfordert Purge-Erkennung in beliebigen Stellungen.
- Fehlersuche für Stellungen > 26 Steine (Speicher: Tabelle größer als 2^24 nötig).
